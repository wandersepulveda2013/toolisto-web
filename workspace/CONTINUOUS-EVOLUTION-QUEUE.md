# CONTINUOUS-EVOLUTION-QUEUE.md — Backlog de Evolucion Continua

> Backlog dinamico: OpenCode anade tareas descubiertas (DISCOVERED) y actualiza estados.
> Estados: `TODO` / `ACTIVE` / `BLOCKED` / `DONE` / `DISCOVERED` / `DEFERRED`.
> Prioridades: `P0` > `P1` > `P2` > `P3`.
> Regla: la siguiente tarea ejecutable es la de mayor prioridad con estado `TODO`.
> Regla de salud: si >50% de los ultimos 10 ciclos fueron AUDIT_ONLY, el siguiente ciclo (salvo
> P0/P1) debe ser una mejora de producto.
> Updated: 2026-09-08 (D-19 DONE: CE-143 cerrado — `documentBlocksToSections` conserva `numbered-list` (li→prefijos `1. `/`2. `/...), `code` (contenido crudo) y `callout` (`> Nota: ...`) en informe/PDF/Markdown; suite nueva document-blocks-sections-semantics 39/39 registrada en el gate; presupuesto dist recortado de paso: 11 clases CSS muertas eliminadas de `workspace.css` (214315 → 208678 bytes, −5637) → workspace-test 157/157 con dist 1194KB <1200KB; RELEASE GATE completo 136 suites PASS 0 fail. Mision SEO publica (directiva del owner): hotfix `/offline` (noindex+description) + 5 editoriales añadidas a `buildSitemap` (sitemap 224 URLs); `audit-content-quality` FINAL PASS pass=15 warn=0 fail=0; evidencia en `artifacts/adsense-content-remediation/`)
> Previo: 2026-09-08 (D-18 DONE: CE-137 cerrado — la funcion muerta `faithfulOcrText` (promesa de resaltado de palabras dudosas que nunca renderizaba) y la clase CSS huerfana `.ws-ocr-low-confidence` ya NO existen en el bundle fuente (0 ocurrencias en workspace.js y workspace.css; removidas en 5a3629f pero la tarea nunca se cerro). D-18 verifica la eliminacion con suite dedicada, limpia la condicion redundante que escribia `doc.ocrWords` dos veces en `finalizeExtraction` (L3167 subsumida por la linea general) y certifica que la cadena VIVA `doc.ocrWords -> ocrWordConfidenceMap -> buildCellConfidenceMatrix -> table.cellConfidence` sigue cableada (confianza por palabra alimenta la matriz de revision). Suite nueva ocr-low-confidence-dead-code 23/23 registrada en el gate; regresiones OCR/resen verdes; gate completo 135 suites PASS 0 fail; commit 6893cff)
> Previo: 2026-09-08 (D-17 DONE: CE-148 — Ctrl+A en la vista de tabla ya selecciona y copia TODA la tabla. El handler (workspace.js ~L5627) hacia `setSelection(0,0,false)` (anchor y focus en la esquina) y rellenaba el mecanismo muerto `selection.endRow/endCol`, que `tableSelectionBounds` no leia: bounds de 1 celda, Ctrl+C copiaba 1 celda. Fix: `setSelection(0,0,false)` + guard de tabla no vacia + `setSelection(rows-1, cols-1, true)` (focus REAL al final); eliminadas las escrituras muertas endRow/endCol y la llamada manual a markTableSelection (setSelection la invoca). Suite nueva table-select-all 16/16 registrada en el gate; regresiones enfocadas verdes; gate completo 134 suites PASS 0 fail; commit 3ad2fdd)
> Previo: 2026-09-08 (D-16 DONE: CE-149 R3 — `queryRunOperation` pasa de hasta 3 barridos por paso (queryCloneShape + `rows.map(row=>[...row])` copia + transform por columna) a 2, mutando en sitio el clon profundo con transform por columna en una sola pasada de filas (fill-down/up conservan su pase secuencial por columna); `queryApplyStep` aplica SOLO el paso nuevo al modelo materializado O(filas) en vez de re-ejecutar toda la cadena O(filas x pasos) — `queryRebuildModel` sigue siendo autoridad de undo/reset/quitar paso (& restaura vía queryModelFromSaved). Suite nueva query-single-pass 20/20 (anclas estaticas + equivalencia incremental==replay en cadenas y en cada paso intermedio + aislamiento de mutacion + fill secuencial) registrada en el gate; gate completo 133 suites PASS; commit f2616cb)
> Previo: 2026-09-08 (D-15 DONE: CE-145 R4 — markTableSelection pasa de barrer toda la grilla O(N) por tecla a marcado incremental O(celdas cambiadas) con tableSelectionDiff/forEachGridCellInRect + indice fila→tr por tbody; suite table-selection-incremental 28/28 registrada en el gate; CE-145 dividida: R3 (queryRunOperation barrido triple) queda como CE-149 (TODO); hallazgo nuevo CE-148 (Ctrl+A solo marca la celda 0,0) DISCOVERED; gate 133 suites PASS, commit 01c0765)
> Previo: 2026-09-07 (D-14 DONE: undo/redo global no rompe el scope de rerenderTable — fix 8e8c79c + suite data-table-undo-scope 10/10; progreso parcial CE-143 commiteado en fca56a4 sin cerrar la tarea; gate 132 suites PASS)
> Previo: 2026-09-06 (TestCycles T2: cierre del paso pendiente de T1 — commit 62c7182; primera validacion en produccion de la deteccion de bucle de narracion CE-069; sin cambios de estado de tareas)
> Previo: 2026-09-06 (TestCycles T1: verificacion del estado del runner `artifacts/autonomous-runs` + commit de infraestructura b73c2a2; sin cambios de estado de tareas)
> Previo: 2026-09-05 (Cycle 199: DISCOVERY 22va ronda (3 exploradores: producto/calidad/rendimiento) cerrada con 11 hallazgos DISCOVERED (CE-137..CE-147, P1x2/P2x5/P3x4); CE-136 DONE — un bloque con salto de página mezclado ya no pierde su texto en informe/PDF/Markdown; gate 129 suites PASS 0 fail por manifest release-gate-72d1015...json)

## Tabla
>
> CE-048 DONE: los resultados de imagen de un flujo se anaden al Workspace como capturas; el payload
> envuelto del engine se normaliza para que document/data se persistan en un flujo real (Cycle 113).
>
> CE-049 DONE: los resultados `text` de un flujo (salida OCR de `image.ocr`) se anaden al Workspace
> como documento Toolisto con id estable derivado del contenido (Cycle 114).
>
> CE-050 DONE: las capturas (imagenes escaneadas) del proyecto se encadenan a Flujos por referencia
> y llegan hasta el OCR real, cerrando `archivo → escaneo → OCR → documento` (Cycle 115).

| ID | Prioridad | Estado | Area | Tarea | Evidencia |
|----|-----------|--------|------|-------|-----------|
| CE-067 | P0 | DONE | Sistema autonomo | Convertir el sistema autonomo (runner/watchdog/lock) en un runtime con estado persistente, progreso verificado y deteccion de bucle de narracion: resolver la raiz del "Let me read the QUEUE" (la narracion avanza el LastWriteTime del log pero no ejecuta -> invisible al launcher/watchdog) con una senal maquina-comprobable | Cycle 130: nuevo `AI_AUTONOMY/` (guard.mjs, state.mjs, runner.mjs, lock.mjs, commit-guard.mjs, queue.mjs + README) y watchdog ampliado. Narracion != progreso: un evento `verified` SOLO se escribe cuando una herramienta real corre; el guard detecta bucles (REPETITION / CONSECUTIVE_INTENTS / RATIO_ZERO_VERIFIED) e interrumpe el ciclo; `state.json`+`events.jsonl` dan recuperacion ante crash (FRESH/RESUME/COMPLETE_TRACKERS/RECOVERY/FRESH-bump) y propiedad de ficheros en commit. Tests nuevos `ai-autonomy-orchestrator-test` 85/85 + `ai-autonomy-resilience-simulation` 17/17 (6 escenarios: verified-vs-narracion, crash+restart, bucle, archivo extrano, determinismo, lock instancia unica) registrados en run-all. La integracion del runner en el bucle PS1 queda documentada como proximo paso |
| CE-068 | P0 | DONE | Sistema autonomo | Integrar el runtime `AI_AUTONOMY` (CE-067) en el launcher real `RUN-OPENCODE-AUTONOMOUS.ps1` para que las ejecuciones reales de OpenCode queden gobernadas por estado persistente: progreso verificado, interrupcion de bucle de narracion, recuperacion ante crash, presupuesto de reintentos, backoff de crash-loop, SAFE_MODE, lock de instancia unica y heartbeat | Cycle 131: el launcher REAL ahora CONSUME el runtime por ciclo (boot -> SAFE_MODE/BLOCKED_OWNER antes de lanzar; start-cycle -> seed RUNNING; heartbeat -> liveness para el watchdog externo; finish (exit 0) o fail+recover (exit != 0) -> backoff persistido via runtime). Los contadores de crash-loop y el presupuesto de reintentos ya NO se reinician al reiniciar el runner (regla CE-068) porque el runtime persiste en `AI_AUTONOMY/runtime.{json,schema}`; SAFE_MODE detiene el runner tras el umbral de crash-loop. Nuevos `AI_AUTONOMY/{runtime.mjs, supervisor.mjs, cli.mjs, fake-opencode.mjs, fake-runner.mjs}`. Pruebas de integracion real (proceso controlado): `ce068-runtime-integration-test` 36/36 (casos A-J: fresh boot, resume, COMPLETE_TRACKERS, bucle de narracion, crash, crash-loop->SAFE_MODE, instancia unica, lock stale, archivo extrano, ciclo exitoso con HEAD real movido) y `ai-autonomy-runtime-policy-test` 45/45 (retry budget, backoff 1/2/5/10, SAFE_MODE, phase stall, verified dect, heartbeat, restart-no-reset). Watchdog ampliado para leer `heartbeat.json` como fallback externo (sin competir con el launcher). Parse OK de ambos .ps1. Registrados en run-all |
| CE-069 | P0 | DONE | Sistema autonomo | Supervisar las ejecuciones reales de OpenCode EN VIVO dentro del runner de produccion: el launcher lanzaba `opencode` bloqueante y el runtime solo actuaba tras la salida del proceso (bucle de narracion "Let me read the QUEUE" =*proceso colgado para siempre*), sin poder intervenir durante la ejecucion | Cycle 132: `RUN-OPENCODE-AUTONOMOUS.ps1` DELEGA la supervision en `cli supervise` (autoridad unica del hijo vivo): interrumpe con gracia (child.kill, graceMs) y SIEMPRE limpia el arbol completo (`terminateTree`: killTree `taskkill /PID /T /F`, idempotente, sin huerfanos, §9 Windows no mata descendientes con kill()), clasifica NARRATION_LOOP (repeticion/intentos consecutivos, `LOOP_INTERRUPTED`) vs STALL (budget de fase SIN progreso verificado, salida 3) vs CRASH (exit != 0), heartbeat LIVE durante la ejecucion (classification, intents, verified, loopCount, crashLoopStreak), progreso verificado SOLO por senales reales (HEAD movido/fichero propio), policia anti-falso-positivo (stdout OJO no es progreso), fallo interno del supervisor = CONFIG_ERROR persistido + checkpoint preservado + retry via recover SIN des-supervisar en silencio, recovery prompt compacto de un solo uso, escalado (2 fallos seguidos -> backoff + contador; 3 -> SAFE_MODE/break con `RESULTADO_CICLO=SAFE_MODE`), divergencias-no-dualidad en la falla (recover solo, sin doble fail/finish), watchdog no mata cuando el supervisor esta vivo (heartbeat `supervisor:'live'`). Fix de carrera real encontrado por la suite: la muerte del hijo durante un interrupt deliberado se clasificaba CRASH antes de que terminateTree terminara -> flag `interrupting` (close/error ya no clasifican mientras se interrumpe). Suite nueva `ce069-live-supervision-test` 66/66 (A-L: normal, bucle LIVE, legit sin falso positivo, silent->STALL, loop->recover->SUCCESS, 3xloop->SAFE_MODE, arbol parent/child/grandchild sin huerfanos, crash, HEAD real refresca verified, fichero extrano no cuenta, binario inexistente->CONFIG_ERROR+checkpoint, launcher delega en cli supervise; escenarios controlados S1-S5 + heartbeat). Regresion CE-068 36/36, policy 45/45, orchestrator 85/85, resilience 17/17. AST parse OK PS1. Smoke real = SKIPPED_EXTERNAL_CONSTRAINT (harness deniega el binario `opencode`; evidencia `artifacts/ce069-smoke-evidence.json`; la ruta de produccion queda certificada por el caso L + subprocesos reales S1-S5/HB). Runtime artifacts gitignored. |
| CE-070 | P0 | DONE | Sistema autonomo | Optimizacion autonomica basada en evidencia: el sistema debe aprender de su propio historial operativo (ciclos, outcomes, fallos, recuperaciones) para priorizar tareas, recomendar timeouts y evitar el churn sin intervencion humana; decisiones deterministas, explicables, acotadas y reversibles | Cycle 133 del plan: `AI_AUTONOMY/history.mjs` (modelo de ciclo + storage JSONL tolerante a corrupcion, clasificacion de outcome/valor determinista, penalizacion historica diferenciando fallo de TAREA vs ENTORNO, scoring explicable `scoreTaskDetail`/`selectTaskExplainable`/`recommendTask`, analisis y recomendacion de estrategias de recuperacion, `recommendTimeout` robusto (percentiles p90/p50, nunca media; <10 muestras = INSUFFICIENT_EVIDENCE, sin auto-ajuste), deteccion de context bloat, LOW_VALUE_ACTIVITY, metricas de productividad y policy versionada con rollback `policyFileShape/readPolicy/writePolicy/applyPolicyChange/rollbackPolicy`) + puente CLI (`history record/list`, `metrics`, `recommend`, `tune` recomendacion-only por defecto, `policy show/apply/rollback`) + integracion real en `RUN-OPENCODE-AUTONOMOUS.ps1` (cada ciclo terminado se registra con outcome, duracion, tamano de prompt fresh/recovery, salida y nextAction; fallo de infra supervisor = env-failure no penalizador). Suite nueva `ce070-history-learning-test` 117/117 (A-O: clasificacion, valor, low-value, penalizacion env-vs-task, scoring explicable, seleccion determinista, recomendacion todo/discovery, backtest, timeouts robustos + bounds, bloat, recovery stats, storage con corrupcion aislada, metricas, policy versioning/rollback, CLI e2e + perf 3000 ciclos). Regresion verde: CE-067 85/85, CE-068 36/36, CE-069 66/66, resilience 17/17. Limites documentados: auto-apply OFF por defecto; duracion por fase subrogada por duracion total del ciclo mientras el launcher no mida fases |
| CE-071 | P2 | DONE | Flujo estrella | Bug: `convertDocToTable` («documento → tabla») rompe tablas OCR alineadas por columnas: `detectSeparator` devuelve `''` para tablas multi-espacio, `line.split(' ')` crea columnas fantasma (`Columna 2`, `Columna 4`) y celdas vacias en el eslabon estrella `documento → tabla`. Solucion: reutilizar `parseTabularText` (puro, ya probado, usa `split(/\s+/)` + `reconstructWhitespaceRow`) conservando confianza/relaciones | Cycle 134: `convertDocToTable` delega en `parseTabularText` (import directo en `workspace.js`), aplica `normalizeOcrNumber` post-parsing (conserva el fix del signo OCR `1-`→`-`), conserva la matriz de confianza y la relacion `source-document`, y se elimina la funcion muerta `rebuildTableRow`. Suite nueva `doc-to-table-multispace-test.mjs` 20/20 (tablas multi-espacio sin columnas fantasma, reproduccion del fixture Star-Flow 5 filas/15 celdas, delimitadores `;` y `|`, checks estaticos de la delegacion) + registro en el release gate. Persistence-lifecycle-audit (§19f) robustecido de numero de linea absoluto a findIndex (el cambio de tamanio de workspace.js rompia el check). RELEASE GATE completo 33/33 PASS incl. Star-Flow E2E con OCR real y BOM CSV. |
| CE-072 | P2 | DONE | Flujo estrella | Bug: `tabla → gráfico` (data.to-chart / report.create) usaba una copia local divergente `parseLocaleChartNumber` que interpretaba fechas/horas como numeros (`15/01/2024` → 15012024, `14:30` → 1430), perdia el signo en parentesis `(1.234,56)` y la escala de `%`, de modo que la MISMA tabla daba graficos distintos segun el punto de entrada (boton UI vs flujo) | Cycle 135: `workflow-operations.js` elimina `parseLocaleChartNumber` y `tableChartSeries` usa el parser canonico `parseLocaleNumber` (importado de `core/locale-parser.js`, contrato documentado «All modules must use this instead of ad-hoc parsing») — identico a la ruta UI `tableChartData` de workspace.js: paridad punto a punto. Suite nueva `chart-series-locale-test.mjs` 18/18 (fechas/horas rechazadas como columna numerica, `(1.234,56)` → -1234.56, `5%` → 0.05, millares `1.500,25` → 1500.25, tabla limpia T1..T3 sin regresion, sin columna numerica → aviso, anti-regresion estatica sin strip ad-hoc `[^\d,.+\-()]`) registrada en el release gate. RELEASE GATE completo 34/34 PASS incl. Star-Flow E2E con OCR real. |
| CE-073 | P2 | DONE | Informe PDF | Bug: `informe → PDF` (document.to-pdf) distorsionaba y desbordaba la pagina con imagenes anchas: `normalizePdfImageSections` escribe width/height en px del canvas y `renderImagePDF` recortaba el ancho a `contentW` pero conservaba el alto crudo — una captura 1600x900 se dibujaba 481.6x900 (ratio 0.53 vs 1.78) y el cajon sobresalia por arriba (y negativo) | Cycle 136: `pdf-generator.js` re-escala el alto proporcional cuando el ancho se recorta (`displayH = displayW * image.height / image.width`) y `estimateSectionH` estima el alto coherente para imagenes anchas (evita paginas casi vacias por reserva de 900 pt). Suite nueva `pdf-image-aspect-test.mjs` 11/11 (pure, JPEG estructural SOF0 1600x900 y 200x100 verificado por `readJpegDimensions` real): ancho recortado 481.6, ratio 1600/900 conservado, caja dentro de la pagina, imagen estrecha sin deformacion, fallback sin width/height con aspecto coherente + anti-regresion estatica. RELEASE GATE completo 38/38 PASS (34 previas + CE-072 + CE-073 + CE-071 + CE-064). |
| CE-074 | P2 | DONE | Informe PDF | Bug: `tabla → PDF` con celdas largas/filas irregulares: (a) una celda con URL/token sin espacios se dibujaba en UNA linea hasta 722 pt en una pagina de 595 (se escapaba por el borde derecho); (b) la altura de fila era fija (20 pt) aunque la celda tuviera varias lineas, solapando filas y mandando texto fuera de pagina | Cycle 137: `pdf-generator.js` introduce `cellLines` (envuelve por espacios y ademas corta tokens mas largos que la columna) + `tableRowHeight` (alto real = max lineas * 12.6 + 4, nunca menos de 20 pt) compartidos por `renderTablePDF`, `estimateSectionH` y `addTableSections` (paginacion que encaja filas por su alto real, no por multiplos de 20). Suite nueva `pdf-table-wrap-test.mjs` 18/18 (pure): URL larga partida sin sobrepasar el borde, fila alta 54.4 pt con grilla coherente, tabla limpia sin regresion (grilla 20 pt/fila), multi-pagina con filas altas sin duplicar ni omitir, y anti-regresion estatica. Suites huerfanas previas revalidadas: `pdf-table-pagination-test` 7/7 y `workflow-document-pdf-test` 66/66. RELEASE GATE completo 39/39 PASS. |
| CE-075 | P2 | DONE | Informe PDF | Bug: `informe → PDF` con muchas series (tabla con >= 16 filas): el pitch fijo `barW + 4` hacia que las barras del grafico se dibujaran fuera del area de contenido (566.7 pt con 16 series) e incluso fuera de la pagina A4 (606.3 pt con 20) | Cycle 138: `renderChartPDF` re-escala `barW` para que la ultima barra quede dentro de `contentW`, y si ni con el ancho minimo (10 pt) caben todas las series, recorta a lo que quepan y anade el marcador "+N" (las series ocultas no se silencian). Suite nueva `pdf-chart-overflow-test.mjs` 15/15 (pure): 6/16/20 series enteras y encajadas, 80 series recortadas con cuentas de "+N" exactas, ningun rect fuera de la pagina, anti-regresion estatica. RELEASE GATE completo 40/40 PASS. |
| CE-076 | P2 | DONE | Informe PDF | Bug: `informe → PDF` (document.to-pdf) desbordaba texto por dos vias: (a) `wrapText` no partia tokens mas largos que la linea (URL/nombre largo) — un token de 130 chars en el titulo se dibujaba en UNA linea a 24 pt (~1560 pt de ancho en una pagina de 595) saliendose por el borde derecho; (b) `estimateSectionH` reservaba altos FIJOS (title=36) o contaba lineas con `lineHeight` 14 cuando el render usa `fontSize*1.4` (16.8 pt texto, 33.6 titulos) — el texto de varias lineas se dibujaba mas alto de lo reservado, solapando la seccion siguiente y cayendo al final de pagina en el margen inferior | Cycle 139: `wrapText` corta tokens mas largos que `charsPerLine` (mismo criterio que `cellLines` de CE-074) y `estimateSectionH` usa la altura real del render (`size + (lines-1)*size*1.4 + 8`, via `estimateTextSectionH`) para title/subtitle/date/footer/text; se elimina la constante muerta `lineHeight`. Suite nueva `pdf-text-wrap-test.mjs` 9/9 (pure): URL larga partida en varias lineas dentro del borde, titulo de 130 chars en 4 lineas dentro del area y con el texto siguiente apilado debajo (sin solape), parrafo que cierra pagina sin caer en el margen inferior, anti-regresion estatica. Suites huerfanas de PDF revalidadas: `pdf-table-pagination` 7/7 y `workflow-document-pdf` 66/66. RELEASE GATE completo 41/41 PASS. |
| CE-077 | P2 | DONE | Informe PDF | Bug: `informe → PDF` (document.to-pdf) reservaba para la seccion `chart` una altura O(n): `30 + n*18 + 40`. El render del grafico, en cambio, tiene altura FIJA (titulo ~12 pt + chartH 100 + etiquetas, independiente del numero de series). Con <= 5 series la estimacion INFRA-reservaba (una sola serie reservaba 100 pt pero el render dibuja ~150: el texto siguiente se imprimia SOBRE la barra — probe: barra 655.3..755.3 con baseline del texto siguiente en 673.3); con 20 series SOBRE-reservaba ~430 pt de hueco vacio y con 80 (~1510) empujaba el contenido posterior a otra pagina casi vacia (agravado por el recorte de CE-075) | Cycle 140: `estimateSectionH` para `chart` pasa a altura real derivada del render: `Math.max(150, 30 + (tituloLines-1)*16.8 + 120)` (150 pt para titulo corto, +16.8 por linea extra del titulo). Nunca infra-reserva (sin solape) y ya no empuja huecos: con 80 series chart + texto cabe en UNA pagina. Suite nueva `pdf-chart-layout-test.mjs` 10/10 (pure): chart de 1/20/80 series con texto siguiente — baseline del texto siempre bajo las barras (sin solape), barras dentro de la pagina, chart(20) y chart(80)+texto en 1 pagina, anti-regresion estatica (estimacion O(n) eliminada). Suites huerfanas revalidadas: `workflow-document-pdf` 66/66 (incluye chart) y `pdf-table-pagination` 7/7. RELEASE GATE completo 42/42 PASS. |
| CE-078 | P2 | DONE | Informe PDF | Bug: `informe → PDF` (document.to-pdf) no limitaba la ALTURA de las imagenes: `renderImagePDF` acotaba solo el ancho (`displayW = min(contentW, sectionW)`) y usaba el alto crudo re-escalado por ancho. Cuando el ancho pedido ya cabia (captura estrecha, o `sectionW <= contentW`), una imagen muy alta se dibujaba SIN limite superior: el cajon quedaba por debajo del margen inferior (se recortaba visualmente) y la estimacion (que solo re-escalaba cuando `sectionW > contentW`) no coincidia con el render, generando paginas casi vacias o imagenes truncadas. Aun encajada por ancho, el alto del cajon podia exceder `usableH` | Cycle 141: nuevo `fitImageDisplay(sectionW, sectionH, contentW, usableH)` compartido por `estimateSectionH` (rama image) y `renderImagePDF` (via `context.usableH`, propagado desde `generatePDF`); encaja la imagen dentro del area usable conservando la proporcion (primero por ancho, luego por alto; sin escalar cuando ya cabe). Suite nueva `pdf-image-fit-test.mjs` 16/16 (pure, JPEG minimo): estrecha-alta (400x5000) y alta (768x6000) encajadas por alto dentro del margen, ancha (3000x800) por ancho, imagen que cabe no agrandada, proporcion conservada en todos; anti-regresion estatica. Suite `pdf-image-aspect` (CE-073) actualizada a la nueva implementacion compartida: 11/11. Suites huerfanas revalidadas: `workflow-document-pdf` 66/66, `pdf-table-pagination` 7/7, `pdf-text-wrap` 9/9, `pdf-chart-layout` 10/10. RELEASE GATE completo 43/43 PASS. |
| CE-079 | P2 | DONE | Parser de instrucciones | Bug: el parser resolvia el formato de DESTINO de una conversion con `detectFormat`, que recorre los aliases en orden de definicion (jpg, jpeg, png, webp...) y devuelve el PRIMERO presente en el texto. Cuando la instruccion nombraba el formato FUENTE y el DESTINO, la fuente podia ganar: `convierte este jpg a webp` (jpg antes que webp en el orden) resolvia a image/jpeg -> la imagen se "convertia" a jpeg (sin cambio) en vez de a webp. El formato de origen quedaba inline ademas rompia el sinonimo exacto: `pasa esta imagen jpg a png` no reconocia NI la conversion (ningun intent) porque 'pasa esta imagen a' no matchea con el formato entre 'imagen' y 'a' | Cycle 142: nuevo `detectDestinationFormat(normalized)` que prioriza el alias que aparece DESPUES de una preposicion de destino (" a ", " en ", " a formato ") y cae a `detectFormat` como respaldo; se usa en convert/compress/to-pdf y strip-metadata y en la ambiguedad de convert. Ademas, en `findActions` un patron regex tolera un token de formato entre 'imagen'/'imagenes' y 'a' ("pasa esta imagen jpg a png" ya reconoce convert). Suite nueva `instruction-parser-dest-format-test.mjs` 17/17 (pure): fuente+destino gana el destino (jpg->webp produce webp, png->gif gif, webp->jpg jpeg, jpg->png png), destinos unicos conservados (convertir a jpg, a formato png, conviertelas a webp, convertir a pdf, strip-metadata a jpg), sin formato -> warning+PNG, anti-regresion estatica. Suite heredada `instruction-parser-test` 116/116 sin regresion. RELEASE GATE completo 44/44 PASS (una carrera transitoria en multi-tab-concurrency CE-060, suite de timing de autosave, verde en re-ejecucion x3; no relacionada). |
| CE-080 | P3 | DONE | Higiene de pruebas | Tres suites del camino PDF eran HUERFANAS: `pdf-table-pagination-test.mjs` (7/7), `workflow-document-pdf-test.mjs` (66/66: document.to-pdf end to end con tabla/grafico/imagen) y `pdf-images-shared-test.mjs` (12/12: normalizacion compartida de imagenes PDF, CE-037) NO estaban registradas ni en `tests/run-all.mjs` ni en `scripts/test-workspace-release.mjs`. Se venian re-ejecutando a mano en cada ciclo (por la red de seguridad tras los cambios de paginacion), pero al no estar en el gate su regresion era invisible para la CI | Cycle 143: se registran las tres orphan suites en `scripts/test-workspace-release.mjs` (tras `pdf-image-fit`), con comentario que aclara su rol. Revalidadas en aislamiento (7/7, 66/66, 12/12) e integradas: RELEASE GATE completo 45 suites PASS 0 fail (nuevo). Elimina el riesgo de deriva/regresion silenciosa de estas suites que cubren el camino `document.to-pdf` y la normalizacion compartida de imagenes. |
| CE-081 | P3 | DONE | Higiene de pruebas | La suite `tests/workspace/invoice-fields-test.mjs` (extraccion de campos de una factura del flujo estrella: `text.invoice-fields` opera sobre el texto OCR, 29 checks) era HUERFANA: no estaba registrada ni en `tests/run-all.mjs` ni en `scripts/test-workspace-release.mjs`, igual que las suites PDF de CE-080. Al intentar ejecutarla en aislamiento (el gate y run-all la ignoraban) fallaba con `ReferenceError: WORKFLOW_DEFINITION_VERSION is not defined`: `workflow-model.js` importa esa constante de `schema-versions.js`, pero el test usa `stripImports()` (borra todos los `import`, incluido el de `WORKFLOW_DEFINITION_VERSION`) y la constante del sandbox quedaba indefinida -> la suite solo podia "pasar" si se ejecutaba en un contexto ajeno que definiera la constante. Es el mismo tipo de deuda de cobertura que se ataco en CE-065 y CE-080: una suite que cubre el camino factura del flujo estrella existia pero su regresion era invisible | Cycle 144: se inyecta `WORKFLOW_DEFINITION_VERSION: 1` en el sandbox del test (`sandbox` + `sandboxArgs` del `new Function`, mismo idiom de inyeccion que `model-fk-test.mjs` usa para la version de esquema). Tras el fix la suite pasa limpia en aislamiento: 29/29 0 fail. Se registra en `scripts/test-workspace-release.mjs` (junto al parser de instrucciones, que es su dependencia natural) con comentario CE-081. RELEASE GATE completo: 46 suites PASS 0 fail (nuevo). Elimina el riesgo de deriva de la cobertura de `text.invoice-fields`. |
| CE-082 | P2 | DONE | Persistencia | Auditoria adversarial determinista del camino de persistencia del editor de documentos, END-TO-END (el flujo REAL `autoSaveDoc -> lock -> saveDoc -> almacen -> reload`), para certificar garantias anti-perdida o descubrir una condicion de carrera real de data-loss | Cycle 145 (auditoria, sin defecto reproducible): suite nueva `document-editor-persistence-race-test.mjs` 21/21 (pure, determinista x3) que extrae `_createSaveLock`, `_createEntityLockMap`, `autoSaveDoc`, `_flushDirtyEntity` REALES de `workspace.js` y los cablea con `appStore` real (createStore), capa persistente en memoria fiel a `storage.js` saveDoc (guard `_writeSeq` coalescing, migrateObject, dbGet/dbPut) y timers manuales; tras cada escenario destructivo re-`loadDoc` de la capa y valida el CONTENIDO EXACTO == ultima edicion logica. CUBRE: LWW por orden logico (edits rapidos, out-of-order, mutacion en vuelo, 3 docs alternos), flush-before-navigate, cambio de documento nunca cruza ids, fallo de escritura no marca guardado + recuperacion, aislamiento por id (20 writes de A no tocan B). VEREDICTO: NO se encontro bug reproducible de data-loss en las rutas vivas (CE-057/058 ya endurecieron lock/stale-write/flush). RISCOS RESIDUALES documentados y probados: (a) fallo transitorio del debounce NO marcado como guardado (edge, no perdida real); (b) `deleteDoc` NO cancela un save en vuelo del lock -> el dbPut puede RESUCITAR un doc borrado (ventana minima: el editor flushea al salir antes de llegar al borrado, que solo vive en la vista Documentos). Se registra en el RELEASE GATE. |
| CE-001 | P1 | DONE | Flujo estrella | Mejorar el flujo PDF -> OCR -> texto -> tabla -> Excel dentro del proyecto: menos pasos, parametros auto-razonables, vistas previas intermedias | Cycle 22: parser tabular reutilizable; conserva etiquetas OCR con espacios, normaliza signo OCR y evita dividir comas decimales. Tests 7/7 + flujo E2E 15/15 |
| CE-002 | P1 | DONE | Flujo estrella | Mejorar el flujo imagenes -> mejorar -> comprimir -> convertir -> ZIP: encadenamiento real y salidas superiores | Cycle 27: operación terminal ZIP real, encadenado de Blob preservado, defaults en UI y descarga compatible. Engine 18/18, UI 30/30, planner 68/68, E2E navegador 19/19 sin errores JS. |
| CE-003 | P2 | DONE | Workspace | Workspace como centro de poder: acceso directo a las 167 herramientas y encadenado de pasos desde el proyecto | Cycle 28: Documentos y tablas pueden abrir el constructor como entrada por referencia local; el selector filtra pasos compatibles y el E2E confirma documento -> flujo sin exportar. UI 33/33, E2E 20/20 |
| CE-004 | P2 | DONE | Producto | Evolucion de las 167 herramientas: reducir pasos, auto-parametros, mejor calidad de salida y vistas previas | Cycle 29: calculadora científica corrige funciones inversas y la constante e, y limita el evaluador a funciones matemáticas permitidas. Gate E2E 24/24 sin red externa ni errores de consola. |
| CE-005 | P2 | DONE | Producto | Defaults inteligentes e integraciones entre herramientas (salida de una como entrada de otra) | Cycle 30: continuaciones locales reales entre conversores tabulares; CSV → Excel → JSON probado E2E sin recarga ni red. Gate hojas de cálculo 203/203 |
| CE-006 | P2 | BLOCKED | Pipeline OCR | Retomar la mejora del OCR del fixture dificil (texto ~8px con ruido): probar preprocesos que superen la via cruda con OEM 3 | Cycle 31: contraste 63/0, umbral 48/0, PSM 4 0/0, PSM 6 76/43 y PSM 11 50/35 (chars/words); ninguno supera crudo OEM 3 76/43 sin degradar. Reabrir solo con mejor modelo o fixture de mayor resolución. |
| CE-007 | P2 | DONE | Fiabilidad | Investigar/reducir el flake de visibilidad de navegacion en `playwright-render.mjs` y `visual-audit-click-nav.mjs` | Cycle 32: el render E2E usa puerto aislable, preview interno estable y localizadores visibles con esperas por contenido; 33/33 PASS en navegador. El audit ignorado se confirmó contra servidor aislado: navegación Documentos/Captura sin corrupción ni errores. |
| CE-008 | P3 | DONE | Rendimiento | Auditar memoria de motores pesados en sesiones largas del Workspace (Tesseract/PDF) y lazy-load de imagenes grandes | Cycle 99: lazy-load real de imagenes de captura con IntersectionObserver: solo se resuelve el dataUrl al entrar la tarjeta al viewport (+300px), <img> con loading=lazy/decoding=async, fallback sin IO. E2E nuevo 12/12; regresion Phase 3A 80/80, Phase 3B 59/59, Workspace 156/156 y Star-Flow 83/83. La auditoria de memoria de motores pesados (Tesseract/PDF) queda como tarea separada DISCOVERED. |
| CE-040 | P3 | DONE | Rendimiento | Auditar el consumo de memoria de motores pesados (Tesseract/PDF) en sesiones largas del Workspace, con medicion repetible y mejora acotada | Cycle 109: `EngineLoader` libera el worker Tesseract.js por inactividad (`setTesseractIdleTimeout`, reaper, `releaseIdleTesseract`) y manualmente (`releaseTesseract`, `destroyAll` refactorizado); `clearFlow` del constructor libera el OCR; el worker se reutiliza en caché y se recarga bajo demanda. Test E2E nuevo `engine-idle-release-test.mjs` 10/10 (Tesseract real, sin mocks) + regresión Workflow UI 39/39, Engine 18/18, Validator 11/11, Registry 26/26, Workspace 156/156, Phase 3A 80/80, Phase 3B 59/59, OCR Source 34/34, determinismo 71/71 y Workflow E2E 20/20. Registrado en el release gate. |
| CE-009 | P3 | DONE | UX/Movil | Revisar accesibilidad y contraste de componentes especializados del Workspace (gates propios) | Cycle 104: contrato ARIA/teclado de los widgets dinámicos del constructor de flujos: filas del selector de operaciones `role=button`+`tabindex`+Enter/Espacio, filtros de categoría con `aria-pressed` (uno activo), y modal «Desde Workspace» en `role=listbox`/`role=option` con `aria-selected` y activación por teclado. Test VM 39/39 + E2E navegador nuevo 11/11 sin errores JS; regresión Workflow E2E 20/20, Phase 3A 80/80, Phase 3B 59/59, Workspace 156/156 y Star-Flow 83/83. |
| CE-010 | P3 | DONE | Docs | Guia de despliegue estatico (GitHub Pages) documentada con build reproducible (heredado de PR-017) | Cycle 33: guía corrige la precedencia real de `productionDomain`, protege rutas de proyecto Pages y usa `node server.js` sin descarga externa; audit 10/10 + build 179/179. |
| CE-011 | P3 | DONE | Privacidad | Mantener el gate de red negativa del sitio publico y del Workspace como regresion permanente | Cycle 189 (CE-011): **suite nueva** `tests/workspace/workspace-network-negative-test.mjs` **57/57 PASS** (7 capas, SIN OCR real): 1) home + creacion de proyecto por UI + flujo real intake→escaner→captura en IndexedDB con cero egress; 2) seed IDB con marcador `TLST-WORKSPACE-NET-CANARY-DETERMINISTIC` + recorrido de las 10 vistas del proyecto sin requests externos nuevos por vista; 3) marcador ausente de URL/body/headers; 4) escaneo estatico de `dist/workspace/**/*.js` (0 primitivas fetch/XHR/sendBeacon/WebSocket/EventSource con URL externa literal); 5) controles positivos por canal (fetch/beacon/img/EventSource interceptados por `context.route('**/*')` + WebSocket bloqueado por stub); 6) evidencia determinista. **`public-site-network-negative.mjs` registrado en el gate publico** (antes solo en run-all.mjs): gate publico 24 PASS 0 FAIL, red negativa 413/413, texto del check saneado (167→«todas las herramientas»). **Determinismo**: evidencia en MIGRATED, 83/83. RELEASE GATE PASS 0 fail, total **119 suites** (`release-gate-c696eef...json`). Commits `c696eef`, `fa86f13` (docs). |
| CE-012 | P2 | DONE | Evidencia | Normalizar los escritores de evidencia (`TLT-*.json` en `tests/` y `scripts/`): salidas deterministas sin timestamps absolutos ni orden aleatorio, para que regenerar = diff cero (eliminar el churn) | `tests/evidence-helper.mjs` + ratchet `tests/evidence-determinism.mjs` 69/69; red negativa con canario fijo y agregados (36 KB, 2 ejecuciones con SHA idéntico); `run-all` 43/43 con diff cero tras regenerar; commit de Cycle 26 |
| CE-013 | P1 | DONE | Informes/PDF | Paginar tablas largas del generador PDF del Workspace, repitiendo encabezados y manteniendo cada fila dentro de los límites de la página | Cycle 35: fragmenta tablas por filas, repite encabezados y conserva todas las filas. Test nuevo 7/7 + Phase 3B 59/59, build 179/179 y sync Workspace OK. |
| CE-014 | P2 | DONE | Rendimiento/escáner | Reducir el coste de la corrección de perspectiva para capturas grandes sin rebajar la calidad de lectura | Cycle 72: `perspectiveCorrectBilinear` evita copiar el canvas fuente, elimina la asignación por píxel de `bilinearSample`, precalcula los términos lineales por fila y hace el muestreo inline con clamping idéntico. Medido en navegador: 104→72 ms (1,7M px), 156→97 ms (3,2M px) y 380→212 ms (6,9M px) con calidad idéntica (Phase 3A 80/80, Phase 3B 59/59, Star-Flow 83/83 con OCR 100/100). |
| CE-015 | P2 | DONE | Accesibilidad/Workspace | Completar el contrato ARIA y teclado de pestañas y selectores especializados del Workspace | Cycle 71: las tres cintas (Documento, Tabla y Query) y el selector de hojas Query implementan el patrón WAI-ARIA tabs: roving tabindex, flechas/Home/End con activación, `aria-controls`/`role=tabpanel`/`aria-labelledby`. Test navegador nuevo 22/22 + regresión 80/80 y 59/59 + Star-Flow 83/83. |
| CE-016 | P1 | DONE | Fiabilidad/escáner | Evitar aplicar una corrección de perspectiva obsoleta si se confirma el escaneo mientras la vista previa asíncrona aún se está recalculando | Cycle 37: revisiones inmutables de vista previa, confirmación serializada y botón deshabilitado hasta el último canvas. Phase 3A 48/48 + Phase 3B 59/59. |
| CE-017 | P2 | DONE | Rendimiento/escáner | Liberar el listener global de redimensionado al cerrar el escáner para que sesiones repetidas no retengan UI y trabajo de layout | Cycle 43 (cubierto por CE-022): `destroy()` elimina el listener `resize` (función nombrada en `setupDragHandlers`) y Phase 3A mantiene el contrato «Scanner removes its resize listener on destruction» (80/80 en Cycle 75). |
| CE-018 | P2 | DONE | Calidad/escáner | Corregir el muestreo de bordes de perspectiva para que el mapeo alcance de forma determinista los límites de la captura sin píxeles transparentes | Cycle 70: muestreo por centro de píxel `(dx+0.5)/outputWidth` con límites reales de la fuente y `bilinearSample` con clamping; 0 píxeles transparentes en cuadrilátero a borde completo y 4 cuadrantes mapeados. Phase 3A 80/80 (5 contratos nuevos), Phase 3B 59/59, Star-Flow E2E 83/83 (OCR 100/100), build 179/179 y sync OK. |
| CE-019 | P3 | DONE | Fiabilidad de pruebas | Hacer aislable el puerto del verificador manual Phase 3A sin alterar sus escenarios de navegador | Cycle 75: `phase3a-manual-verification.mjs` respeta `E2E_PORT` (default 8082) en server, log y `page.goto`. Ejecutado en 8084: 41/41 PASS sin `EADDRINUSE`; regresión Phase 3A 80/80. |
| CE-020 | P1 | DONE | Fiabilidad/escáner | Evitar que una detección o restablecimiento de esquinas que termina tarde sobrescriba el último ajuste manual del usuario | Cycle 40: revisión monotónica de intención para Auto-detectar, Restablecer y arrastre manual; un resultado tardío se ignora. Phase 3A 51/51 (3 contratos de carrera nuevos), Phase 3B 59/59, build 179/179 y sincronización Workspace OK. |
| CE-021 | P1 | DONE | Fiabilidad/escáner | Evitar que Cancelar durante el guardado asíncrono de un escaneo confirme silenciosamente un documento que el usuario ya descartó | Cycle 42: `onConfirm` se espera, Cancelar y Escape quedan bloqueados durante «Guardando escaneo...», y se restauran al completar. Phase 3A 54/54 incluye contrato asíncrono de bloqueo; Phase 3B 59/59, build 179/179 y sync Workspace OK. |
| CE-022 | P2 | DONE | Fiabilidad/escáner | Invalidar la sesión del escáner al cancelar o destruirla para que inicializaciones y vistas previas tardías no monten controles ni listeners sobre una vista ya cerrada | Cycle 43: sesión activa invalidada en `destroy`, referencias de canvas liberadas y listener `resize` eliminado; cancelar y guardar destruyen la UI antes de navegar. Phase 3A 56/56 añade carga diferida destruida y teardown; Phase 3B 59/59, build 179/179 y sync OK. |
| CE-023 | P1 | DONE | Accesibilidad/escáner | Hacer operables con teclado las cuatro esquinas del escáner y anunciar sus coordenadas sin romper el arrastre táctil o de puntero | Cycle 45: cada esquina es un slider enfocable con flechas (5 px; Mayús + flecha 20 px), Inicio/Fin, límites y `aria-valuetext` X/Y actualizado. Conserva arrastre de puntero y añade foco visible; Phase 3A 59/59 con contrato de teclado real en navegador, Phase 3B 59/59 y sync OK. |
| CE-024 | P1 | DONE | Fiabilidad/escáner | Serializar Auto-detectar/Restablecer con Aplicar escaneo para que una detección pendiente no permita confirmar la vista previa anterior | Cycle 47: la detección pendiente es una operación de procesamiento que deshabilita Confirmar y cuya promesa espera Ctrl+Enter; una vista previa anterior no puede persistirse. Phase 3A 62/62 (3 contratos diferidos nuevos), Phase 3B 59/59, build 179/179 y sync Workspace OK. |
| CE-025 | P1 | DONE | Fiabilidad/escáner | Hacer recuperable un fallo al persistir un escaneo: devolver el resultado del guardado al UI y restaurar un estado de error/reintento en lugar de dejar «Guardando escaneo...» tras capturar el error | Cycle 49: `onConfirm` devuelve resultado explícito; el UI muestra error recuperable y permite reintentar. Phase 3A 64/64 (2 contratos de fallo/reintento), Phase 3B 59/59, build 179/179 y sync Workspace OK. |
| CE-026 | P1 | DONE | Integridad/escáner | Persistir atómicamente el resultado completo del escáner (assets, ScanDocument, captura y ejecución) para que un fallo de IndexedDB no deje relaciones huérfanas ni resultados parciales | Cycle 51: `persistScannerResult` prepara relaciones y guarda los tres assets (original, ScanDocument y corregido), captura y ejecución en una sola transacción; los eventos/UI ocurren tras commit. `dbTransaction` aborta ante fallo del callback. Phase 3A 67/67 incluye commit completo y rollback controlado; Phase 3B 59/59, build 179/179 y sync OK. |
| CE-027 | P1 | DONE | Fiabilidad/escáner | Mantener una salida recuperable cuando falla la carga inicial del escáner: conectar Cancelar/Escape y explicar el fallo aunque `processCapture()` rechace antes de instalar controles | Cycle 53: tras el fallo inicial se conserva un mensaje accionable y se conectan Cancelar/Escape. Phase 3A 69/69 incluye los dos contratos de navegador; Phase 3B 59/59, build 179/179 y sync OK. |
| CE-028 | P1 | DONE | Integridad/escáner | Impedir que el ajuste manual genere un cuadrilátero cruzado o degenerado, para que la corrección de perspectiva no pliegue ni vacíe la salida | Cycle 56: valida convexidad estricta y área mínima antes de aceptar un ajuste manual; conserva la última geometría/previsualización válida y anuncia el rechazo. Phase 3A 73/73 (4 contratos de geometría), Phase 3B 59/59, build 179/179 y sync Workspace OK. |
| CE-029 | P1 | DONE | Integridad/almacenamiento | Evitar persistir dos copias grandes de la imagen corregida de cada escaneo: la captura debe referenciar su asset corregido sin volver a guardar su `dataUrl` | Cycle 59: las nuevas capturas guardan solo `correctedAssetId`; el PNG vive una vez en el asset corregido. Tarjetas, OCR y contexto de tablas resuelven el asset, y los proyectos existentes conservan fallback de `dataUrl`. Phase 3A 75/75, Phase 3B 59/59 y Star-Flow E2E 83/83. |
| CE-030 | P1 | DONE | Seguridad de datos/UX | Advertir de forma explícita y proporcional el borrado en cascada de una captura de escáner antes de confirmar, para que no se eliminen sin saberlo el escaneo, OCR, tablas, gráficos, exportaciones y ejecuciones derivados | Cycle 60: vista previa local sin escrituras calcula la cascada antes de abrir el diálogo; el aviso enumera derivados por tipo y se anuncia como alerta. Phase 4b 47/47 en navegador valida cadena completa, preview sin borrado y cero errores; Phase 3A 75/75, Phase 3B 59/59, build 179/179 y sync OK. |
| CE-031 | P1 | DONE | Integridad/importación | Corregir el remapeo de referencias del escáner al importar un proyecto para que captura, ScanDocument y página apunten al asset corregido con su nuevo ID | Cycle 61: `importProject` remapea captura (`scanDocumentId`, `correctedAssetId`), ScanDocument y páginas (`originalAssetId`, `correctedAssetId`, `assetId`); round-trip real Phase 5 53/53 sin huérfanos. |
| CE-032 | P2 | DONE | Flujo estrella | Convertir en el constructor una operación real documento/informe → PDF local, cerrando el flujo estrella (documento → informe → PDF) sin salir del proyecto | Cycle 80-81: `document.to-pdf` registrada en `workflow-operations.js` (categoría pdf, entrada document/text, salida file, opciones formato/orientación/título). Mapea bloques Toolisto (heading1/2/3, párrafos, viñetas, citas, divisores y saltos de página) a secciones del `pdf-generator` local y descarga un PDF válido sin red. Test nuevo `workflow-document-pdf-test.mjs`; ciclo 81 consolida el commit que había quedado sin cerrar en el ciclo 80. Engine 18/18, Validator 11/11, UI workflow 33/33, Planner 68/68, Parser 111/111, Assistant UI 39/39, Workflow E2E 20/20, Phase 3B 59/59, Phase 3A 80/80, Workspace 156/156 y Star-Flow E2E 83/83 con build 179/179 y sync source/dist OK. |
| CE-033 | P2 | DONE | Flujo estrella | Mantener la tabla real del informe cuando el flujo estrella convierte documento → PDF: `report.create` embebe los datos como bloque `table` y `document.to-pdf` lo emite como grilla PDF real (headers + filas) | Cycle 81: `report.create` añade un bloque `table` con `headers`/`rows` cuando la entrada trae esos campos, y `documentBlocksToSections` convierte dicho bloque en una sección `table` del `pdf-generator` (cabecera diferenciada, filas y paginación de tablas largas ya soportadas). Test `workflow-document-pdf-test.mjs` ampliado a 35/35: bloque emitido por `report.create`, grilla renderizada en el PDF, cuerpo posterior al grid y cadena real text.to-table → report.create → document.to-pdf. Regresión Engine 18/18, Validator 11/11, UI workflow 33/33, Planner 68/68, Parser 111/111, Assistant UI 39/39, Workflow E2E 20/20, Phase 3B 59/59, Phase 3A 80/80, Workspace 156/156 y Star-Flow E2E 83/83 con build 179/179 y sync source/dist OK. |
| CE-036 | P2 | DONE | Flujo estrella | `document.to-pdf` debe embeker los `image-block` como secciones `image` reales del PDF (normalizando PNG/WebP/SVG a JPEG como hace `preparePdfImages`), para que un documento con imágenes no vuelque su base64 como texto visible en el PDF | Cycle 84: `documentBlocksToSections` mapea `image-block` a sección `image` (dataUrl) y `document.to-pdf` normaliza PNG/WebP/SVG a JPEG vía canvas (`preparePdfImageSections`), igual que la ruta de diseños; `pdf-generator` embebe `/Subtype /Image` con DCTDecode. Test VM 43/43 (JPEG embebido, PNG sin leak, placeholder explícito) + E2E navegador nuevo 16/16 (PNG/WebP→JPEG real en canvas, sin leak, sin errores JS) + workflow E2E 20/20, Phase 3B 59/59 y Star-Flow 83/83. |
| CE-037 | P3 | DONE | Rendimiento/PDF | Compartir la normalización de imágenes para el flujo `document.to-pdf` con la lógica ya usada por `preparePdfImages` (evitar duplicar el re-encode JPEG por ruta) | Cycle 85: nuevo `core/pdf-images.js` con `normalizePdfImageSections` (re-encode JPEG 0.9 en canvas, passthrough JPEG, placeholder sin DOM); `document.to-pdf` lo usa con `updateSize:true` y `preparePdfImages` de workspace.js con `reportError`. Test VM nuevo 12/12 (dedup por ruta + passthrough sin DOM) + document-to-pdf 43/43, innerHTML 27/27, CE-036 E2E navegador 16/16, workflow E2E 20/20, Phase 3B 59/59 y Star-Flow 83/83 con build 179/179 y sync OK. |
| CE-038 | P2 | DONE | Flujo estrella | Cerrar el flujo estrella `datos → gráfico → informe → PDF`: crear la operación `data.to-chart` que convierte una tabla en un bloque de gráfico de barras y que `report.create`/`document.to-pdf` lo lleven como sección de gráfico real al PDF | Cycle 94: nueva operación `data.to-chart` (categoría chart, entrada data/document, salida document) que elige la columna numérica por ancla (parseo de decimales con coma europea), emite bloque `chart` con series finitas y rechaza tablas vacías con mensaje accionable. `report.create` añade el bloque de gráfico cuando hay columna numérica y `document.to-pdf` lo mapea a sección `chart` del `pdf-generator` (barras `re f`, título y etiquetas). Parser: sinónimos `crear gráfico/grafica/graficar`; Planificador: acción `chart` → `data.to-chart`; UI: categoría «Grafico» y filtro/icono. Parser 116/116, Planner 73/73, Document→PDF 66/66, E2E navegador nuevo 15/15 (cadena text.to-table → data.to-chart → document.to-pdf), Workflow UI 33/33, Registry 26/26, Engine 18/18, Validator 11/11, Assistant UI 39/39, Model 39/39, Tabular parser 7/7, InnerHTML 27/27, build 179/179, sync source/dist OK, Workflow E2E 20/20, Phase 3B 59/59, Workspace 156/156 y Star-Flow E2E real 83/83. |
| CE-041 | P2 | DONE | Flujo estrella | Activar la extracción de campos de factura/recibo: consumir `core/invoice.js` (código muerto) para que la intención `_extractFields` del parser (mención de "factura"/"recibo" con OCR) produzca un paso real de extracción de campos estructurados (Campo/Valor/Confianza/Página) encadenado al OCR | Cycle 101: nueva operación `text.invoice-fields` (entrada text/document, salida data) que ejecuta `parseInvoiceText`+`invoiceRows`; `instruction-planner.js` encadena el paso tras `image.ocr` cuando `_extractFields` es true y la operación está registrada (sin paso fantasma si no lo está). Test nuevo `invoice-fields-test.mjs` 29/29 + regresión Parser 116/116, Planner 73/73, Engine 18/18, UI 33/33, Registry 26/26, Document→PDF 66/66, Tabular 7/7 y sync source/dist OK. |
| CE-042 | P3 | DONE | Flujo estrella | Validar en navegador (E2E) la instrucción real "extrae el texto de la factura" sobre una captura con factura, verificando OCR → campos de factura → tabla y cero errores de consola | Cycle 102: nuevo `invoice-fields-e2e.mjs` 14/14 en navegador real (chromium headless, servidor sobre dist, OCR Tesseract local `spa`): instrucción real → parser marca `_extractFields`, planner encadena `image.ocr → text.invoice-fields`, ejecución completa con factura capturada en canvas (Proveedor/RNC incluidos), 9 campos extraídos, `N.00123`, Total `2950.00`, confianza 86%, cabeza `Campo|Valor|Confianza|Página`, 11 filas, cero errores de consola. Regresión Parser 116/116, Planner 73/73, Invoice-fields 29/29, build 179/179 y sync source/dist OK. |
| CE-043 | P2 | DONE | Producto/Sitio público | Consolidar la migración de dominio toolisto.com heredada: el build de producción genera 179 páginas, sitemap/canónicos apuntan a toolisto.com y el audit SEO pasa 2753/2753, pero el registro del ciclo heredó el cambio de `generate-seo-pages.mjs` (cache-busting `app.js?v=` + recuperación de codificación mojibake) y la actualización del README/DEPLOYMENT a hosting estático con CNAME; falta evidencia determinista y un gate de migración que proteja `productionDomain` y `_headers` | Cycle 105: nuevo gate `tests/toolisto-domain-gate.mjs` 23/23 que fija el contrato toolisto.com (siteUrl y productionDomain exactos sin subdirectorio, sin placeholder/.invalid), la lista `_headers` (HSTS, nosniff, Referrer-Policy, Permissions-Policy, CSP, `/workspace/*` noindex) y su propagación al build (`dist/_headers` idéntico, sitemap/robots en toolisto.com). Evidencia determinista `TLT-toolisto-domain-gate-evidence.json` con regeneración diff-cero; registro en `run-all.mjs` y determinismo 71/71. Regresión SEO 2753/2753 y deployment 10/10 |
| CE-044 | P2 | DONE | Producto/Workspace | Hacer compatibles con Excel las exportaciones CSV del Workspace: las descargas «Exportar CSV» (tabla de Datos en `exportTableCSV` y resultado de Query en `exportQueryResult`) no llevan BOM UTF-8, por lo que Excel con locales con acentos (es) interpreta el archivo como ANSI y muestra mojibake (Ã¡, Ã©…) | Cycle 106: BOM UTF-8 (`\uFEFF`, bytes EF BB BF) antepuesto al contenido en ambas rutas y `;charset=utf-8` en el Blob de la tabla; nuevo E2E `tests/workspace/csv-export-bom-e2e.mjs` 20/20 en navegador real (tabla con `Córdoba/Éxito/índice ñame` y fixture Query con `Reunión Ñuño`: BOM presente, primer carácter `\uFEFF`, acentos intactos, cero mojibake). Registrado en `test-workspace-release.mjs`. Regresión Workspace 156/156, Phase 3B 59/59, Phase 3A 80/80, Star-Flow E2E 83/83 y determinismo 71/71 con build 179/179 y sync source/dist OK |
| CE-045 | P2 | DONE | Producto/Sitio público | Hacer compatibles con Excel las descargas CSV del sitio público (`excelToCsv`/`jsonToCsv`): los Blobs CSV que `ToolProcessors.excelToCsv` y `ToolProcessors.jsonToCsv` generan en `tool-processors.js` no llevan BOM UTF-8, por lo que Excel con locales con acentos (es) interpreta el archivo como ANSI y muestra mojibake igual que el bug del Workspace CE-044 | Cycle 107: BOM UTF-8 (`\uFEFF`, bytes EF BB BF) antepuesto al CSV en ambas convertidoras del sitio público; `parseCsvText` y `csvToExcel`/`csvToJson` limpian un BOM de entrada (SheetJS no lo elimina con `type:'string'`), protegiendo reapertura/continuación; gate de hojas de cálculo 221/221 (14 checks BOM/acentos + continuación BOM→JSON sin filtrar). `aoaToFile` (branch csv) queda sin BOM porque solo reconstruye el *input* de `csvToExcel`/`csvToJson` y añadirlo corrompería la cabecera. Regresión: determinismo 71/71, cobertura 26/26, verify-115 herramientas 132/132, build 179/179 |
| CE-046 | P2 | DONE | Producto/Workspace | `text.export` debe renderizar los bloques de un documento Toolisto a Markdown estructurado (encabezados, listas, citas, divisores, tablas, gráficos e imágenes) cuando el formato es `md`, y a texto plano legible cuando es `txt`, en lugar de aplanar el contenido con `join('\n')` que perdía toda la estructura | Cycle 110: nueva operación de exportación con `blocksToMarkdown`/`blocksToPlainText` (sin DOM) en `workspace/core/workflow-operations.js`: `heading1/2/3`→`#/##/###`, `bullet-list`→`- `, `quote`→`> `, `divider`→`---`, `table`→tabla Markdown `| a | b |` con separador y celdas escapando `|`, `chart`→bloque delimitado `\`\`\`charts` con tabla Etiqueta|Valor, `image-block`→`![imagen](dataUrl)`. La entrada de cadena se exporta verbatim sin cambios. Test nuevo `tests/workspace/workflow-export-md-test.mjs` 30/30 (VM, sin mocks) registrado en el release gate. Regresión Engine 18/18, Validator 11/11, Registry 26/26, Document→PDF 66/66, Workflow UI 39/39, Builder A11y 11/11 y Workflow E2E 20/20 con build 179/179 y sync source/dist OK |
| CE-047 | P2 | DONE | Producto/Workspace | `addResultToWorkspace` (botones «Anadir al Workspace» de los resultados de un flujo) solo actualizaba el estado en memoria (`currentDoc`/`dataTables`) sin persistir el documento o la tabla en IndexedDB, por lo que un resultado de flujo desaparecía al recargar y se perdía el encadenado de la salida de un flujo con el proyecto | Cycle 112: `workflow-ui.js` recibe `saveDoc`/`saveData`/`refreshProjectCounts` vía appHelpers (inyectados desde `workspace.js`), `addResultToWorkspace` pasa a async y persiste el documento (`saveDoc`) o la tabla (`saveData`) al proyecto, añade el elemento al estado `documents`/`dataTables` (deduplicando por id), refresca conteos y evita guardar dos veces el mismo id. `addResultToWorkspace` se expone en la API del UI para testear. Test `workflow-ui-test.mjs` ampliado a 46/46 (7 contratos nuevos: doc persistido, doc en estado, refresh doc, table persistida, table en estado, refresh table y dedup por id). Regresión Workflow UI 46/46, Engine 18/18, Validator 11/11, Registry 26/26, Export-md 30/30 y Workflow E2E 20/20 con build 179/179 y sync source/dist OK |
| CE-048 | P2 | DONE | Producto/Workspace | Los resultados de imagen de un flujo (`image.rotate`/`resize`/`convert`/escáner) solo ofrecían «Descargar» y no se podían añadir al Workspace (CE-047 solo cubría document/data); además, CE-047 leía `result.data.blocks`/`headers` directamente y el engine envuelve cada resultado como `{ data: <payload>, kind, name }`, por lo que la persistencia de documento/tabla se omitía en silencio en un flujo real | Cycle 113: `addResultToWorkspace` normaliza el payload envuelto del engine (`result.data.data`) y añade la rama `kind === 'image'`: persiste el Blob como captura del proyecto (helper `saveFlowImageResult` en `workspace.js`, patrón CE-029: captura referencia `correctedAssetId`, sin duplicar el PNG en IndexedDB) e inyecta `saveImageCapture` en appHelpers. El botón «Anadir al Workspace» aparece ahora también en resultados de imagen. Workflow UI 54/54 (8 contratos nuevos: image envuelto, project id, refresh image, Blob crudo, nombre por defecto, doc/table con payload envuelto, Blob inválido rechazado); Workflow E2E 25/25 (E2E 5 nuevo: flujo de imagen real → Anadir al Workspace → captura persistida en IndexedDB y visible en Capturas tras reload); regresión Engine 18/18, Validator 11/11, Registry 26/26, Export-md 30/30, Workspace 156/156, Document→PDF 66/66, Phase 3A 80/80, Phase 3B 59/59, Storage 17/17, integridad y bundle 47/47 + 53/53, determinismo 71/71, Phase 3 integridad 52/52, Phase 11 106/106, build 179/179 y sync source/dist OK |
| CE-049 | P2 | DONE | Producto/Workspace | Los resultados `text` de un flujo (especialmente la salida OCR de `image.ocr`) solo ofrecen «Descargar» y no se pueden añadir al Workspace: el texto extraído del paso central del flujo estrella `archivo → escaneo → OCR → documento → tabla → gráfico → informe → PDF` no se persiste como documento, rompiendo el encadenado de la salida de un flujo con el proyecto | Cycle 114: `workflow-ui.js` expone el botón «Anadir al Workspace» en resultados `kind === 'text'` y `addResultToWorkspace` añade la rama `text`: `textResultToDocument(name, text)` convierte el payload (string o Blob) en un documento Toolisto de bloques (headings/bullets/párrafos, mismo mapeo que `text.to-document`) con `id: 'flow-text-' + hash32` estable derivado de nombre+contenido, persiste con `saveDoc`, deduplica por id, refresca conteos y abre `currentDoc`. Workflow UI 62/62 (8 contratos nuevos CE-049: wrapped, id estable, estado, refresh, dedup, headings/bullets, nombre propio, texto vacío rechazado); Workflow E2E 31/31 (E2E 6 nuevo con OCR real del fixture `scan-clear.png` → resultado text → Anadir al Workspace → documento con bloques persistido en IndexedDB con las palabras OCR reales y visible en Documentos). Registro de `workflow-ui-test` en el release gate. Regresión Engine 18/18, Validator 11/11, Registry 26/26, Export-md 30/30, Document→PDF 66/66, Workspace 156/156, Phase 3A 80/80, Phase 3B 59/59, OCR Source 34/34, Storage 17/17, integridad 47/47, determinismo 71/71, Phase 3 integridad 52/52, Phase 11 106/106, build 179/179 y sync source/dist OK |
| CE-050 | P2 | DONE | Producto/Workspace | Las capturas (imágenes escaneadas) del proyecto no se podían encadenar a Flujos: CE-003 solo permitía documento/tabla, `startWorkflowFromWorkspace` rechazaba `image` y «Desde Workspace» listaba solo documento/tabla, por lo que la imagen corregida de un escaneo no se podía reutilizar como entrada de un flujo (OCR, rotación…), rompiendo `archivo → escaneo → OCR` | Cycle 115: `workflow-ui.js` lista las capturas en `selectFromWorkspace` (`kind: 'image'`, id `capture-<id>`, icono `camera`), `addWorkspaceItems` acepta `image` por referencia (dedup por workspaceRef) y `executeFlow` resuelve `capture-<id>` a la imagen Blob mediante el helper inyectado `resolveCaptureImage` (patrón CE-029: se resuelve el asset corregido sin duplicar el PNG). `workspace.js`: nuevo `resolveFlowCaptureImage(captureId)` (loadCaptureById + resolveCaptureImageDataUrl + `fetch(dataUrl).blob()`), inyectado en `renderWorkflowView`; `renderCaptureView` añade el botón «Encadenar» a las tarjetas de captura y `startWorkflowFromWorkspace` acepta `['document', 'data', 'image']`. Workflow UI 65/65 (3 contratos nuevos: captura añadida, dedup, ops de imagen compatibles); nuevo `capture-flow-chain-e2e.mjs` 12/12 en navegador real (seed proyecto+captura+asset en IDB → vista Capturas → Encadenar → OCR real del fixture `scan-clear.png` → documento con bloques persistido tras reload con las palabras OCR reales, cero errores/consola y cero requests externos), registrado en el release gate. Regresión Workspace 156/156, Phase 3A 80/80, Phase 3B 59/59, Phase 11 106/106, Registry 26/26, Engine 18/18, Document→PDF 66/66, OCR Source 34/34, determinismo 71/71, build 179/179 y sync source/dist OK; release gate 13/13 |
| CE-051 | P1 | DONE | Fiabilidad/Flujos | executeFlow no tiene try/catch: un error de operación deja la UI con «Ejecutando…» permanentemente, el botón Limpiar deshabilitado y sin feedback de error al usuario | Cycle 116: executeFlow envuelto en try/catch/finally; generation guard impide continuaciones tras Limpiar; cleanupBtn se deshabilita durante la ejecución; progressText y monitorEl se limpian al inicio; errores se muestran en execErrorsEl. |
| CE-052 | P1 | DONE | Fiabilidad/Flujos | retryFailed no tiene reentrancy guard: un segundo click lanza ejecución paralela; no tiene .catch() y falta stale-generation check | Cycle 116: retryFailed con reentrancy guard (retorna si ya está ejecutando); .catch() en la promesa; stale-generation check; botones se re-habilitan en success y failure. |
| CE-053 | P1 | DONE | Fiabilidad/Engine | getSnapshot().total calcula solo settled (completed+failed+cancelled) en lugar del total planificado; cancelled state nunca se alcanza desde el early return tras generation mismatch | Cycle 116: engine añade runTotal (set en run/retryFailed); getSnapshot retorna runTotal como total; cancelled state se asigna antes del early return; retryFailed rechaza si state es running/queued/cancelling; _onTerminated registra cancelled en results. |
| CE-054 | P2 | DONE | UX/Flujos | Resultados PDF/file-blob no muestran Descargar (condición `r.data &&` bloqueaba); resultados file no muestran Anadir al Workspace | Cycle 116: Download button usa optional chaining `r.data?.data`; 'file' añadido a la condición de Add-to-Workspace button. |
| CE-055 | P2 | DONE | UX/Flujos | Monitor limpia y recrea botón Cancelar en cada evento (DOS de nodos); log se pierde entre eventos | Cycle 116: monitor append entries (cap 50) con botón Cancel persistente; execErrorsEl y progressText se limpian al inicio de executeFlow/retryFailed. |
| CE-056 | P2 | DONE | Fiabilidad/Workspace | renderDocumentsView/renderDataView re-render async opera sobre container detachado tras navegación; workflow auto-save timer se filtra al cambiar vista; IntersectionObserver en capturas no se desconecta; exportProjectData no appendea <a> al DOM; capture resolution sin try/catch | Cycle 116: _viewGeneration counter invalida callbacks async stale; workflowAutoSaveTimer limpiado al cambiar vista; IO en capturas verifica generation y se desconecta; <a> append before click; capture resolution wrapped in try/catch con toast. Release gate 14/14 PASS. |
| CE-057 | P1 | DONE | Fiabilidad/Workspace | autoSaveDoc/autoSaveTable debounce (1s) y _setupAutosave interval (5s) llaman saveDoc/saveData concurrentemente sobre la misma entidad; saveCurrentWorkspaceItem (guardado manual) y _flushAndSaveSession (visibility change) también compiten, causando escrituras paralelas en IndexedDB sobre el mismo documento/tabla | Cycle 117: _createSaveLock() serializa escrituras por tipo (doc/table) con latest-wins, failsafe 60s y generation counter para stale drains. autoSaveDoc, autoSaveTable, _setupAutosave, saveCurrentWorkspaceItem y _flushAndSaveSession usan el lock. renderView cancela debounce timers y cleans lock pending. Test autosave-lock-test.mjs 18/18. Regresión: workspace 157/157, phase3a 80/80, phase3b 59/59, phase11 106/106, UI 65/65, registry 26/26. |
| CE-058 | P1 | DONE | Fiabilidad/Workspace | _createSaveLock usa locks per-tipo (_docSaveLock/_tableSaveLock): dos documentos diferentes comparten el mismo lock, así que latest-wins coalescing pierde writes de A al guardar B. renderView cancela timers pero no hace flush de la entidad dirty antes de navegar, causando pérdida de datos al cambiar de vista. saveDoc/saveData no protegen contra stale writes de IDB con _writeSeq | Cycle 118: _createEntityLockMap() genera locks per-entidad (_docLocks/_tableLocks con Map), aislamiento completo entre documentos y tablas diferentes. _flushDirtyEntity() preserva dirty entity antes de navigation. storage.js saveDoc/saveData con guard _writeSeq monotonico: existing._writeSeq > (doc._writeSeq\|\|0) descarta stale writes. setTableReviewStatus persiste via _tableLocks en lugar de autoSaveTable (debounce cancelado por renderView). Lock memory bounded via onIdle/_maybeEvict. Test cross-entity-integrity-test.mjs 55/55; review-status-persistence-test.mjs 15/15; persistence-sequence-cert.mjs included. Regresión: workspace 157/157, phase3a 80/80, phase3b 59/59, phase11 106/106. Release gate 16/16 PASS. Commit dbe9c1f. |


| CE-063 | P3 | DISCOVERED | Privacidad/Externo | Inyeccion edge de Cloudflare WebMCP (`/.webmcp/bridge.js`) en `apluno.com`: confirmada como puente MCP de Cloudflare, inerte (sin `/mcp`), NO intercepta el trafico de la app ni accede a IndexedDB, NO vulnera cero-egress. Accion = decision del dueno/host (divulgacion o quitar la ruta), no cambio de repo | Cycle 126: investigacion read-only + analisis estatico; evidencia en `artifacts/cloudflare-webmcp/investigation.md` + `evidence.json`; 0 refs en repo; clasificacion `CONFIRMED_CLOUDFLARE_EDGE_INJECTION_BENIGN_TO_ZERO_EGRESS` |

| CE-064 | P3 | DONE | Producto/Workspace | `text.to-document` y el editor de documentos pierden las tablas Markdown (limitacion de CE-046): la conversion `archivo -> ... -> OCR -> documento` no puede continuar el encadenado hacia `tabla` si el texto fuente usa tablas GFM | Cycle 127: `blocksFromText` puro (sin DOM) detecta tablas GFM (separador `:?-+:?`), fences con lang, citas `>`, headings, bullets y parrafos (escapa `\|` -> `|`); `text.to-document` lo usa. `renderBlock` anade rama `table` (tabla real thead/tbody) y `exportDocument` emite tabla GFM. Suite nueva `text-to-document-test` 15/15 registrada en el gate. De paso se corrigio `verify-workspace-sync.mjs` (bug heredado de la capa 3, commit 95aac07): ya refleja la exclusion intencional de docs privados de `dist`, con lo que el release gate volvio a pasar 32/32. Commit e0eb3d1. |

| CE-065 | P3 | DONE | Fiabilidad de pruebas | Deuda preexistente (documentada desde Cycle 117): `workflow-engine-test.mjs` y `instruction-planner-test.mjs` crasheaban al ejecutarse en aislamiento por dependencias sin resolver en el harness de VM (`createExecutionResources` de `execution-resources.js` y `WORKFLOW_DEFINITION_VERSION` de `schema-versions.js`), y no estaban registradas en el release gate (deuda oculta que nadie ejecutaba) | Cycle 128: `workflow-engine-test` incluye `execution-resources.js` en el codigo combinado (18/18 en aislamiento) y `instruction-planner-test` incluye `schema-versions.js` (73/73); ambos + `instruction-parser` (116/116) se registran en `test-workspace-release.mjs` para que queden protegidos contra regresion. Release gate completo 34 suites PASS (build 214 paginas, sync OK, OCR E2E real). Resultado: MEANINGFUL_TEST_COVERAGE. Commit 8e47034. |
| CE-083 | P2 | DONE | Flujo estrella | Los `lineItems` de una factura se calculan pero se descartan: `parseInvoiceText` (invoice.js:144) devuelve `lineItems` (parseLineItems, invoice.js:112-129), pero la operacion `text.invoice-fields` (workflow-operations.js:393-399) solo emite `invoiceRows` (11 campos cabecera) y nunca expone los renglones de compra, rompiendo `archivo -> escaneo -> OCR -> documento -> tabla` para facturas | Cycle 147: `text.invoice-fields` emite tambien `lineItems` `{headers:['Descripcion','Cantidad','Precio unitario','Importe'], rows}` derivado de `parsed.lineItems` (mantiene headers/rows/name/confidence intactos); `addResultToWorkspace` rama data persiste los renglones como tabla propia de id estable `flow-invoice-items-<hash>` con dedup y refresh de counts cuando hay renglones. Verificacion: invoice-fields 34/34, workflow-ui 71/71, invoice-fields e2e 16/16 (renglon `Servicio de diseno` extraido del OCR real), release gate OK. Commit 398330a. |
| CE-084 | P2 | DONE | Flujo estrella/PDF | `pdfString` (pdf-generator.js:59-70) escapa todo caracter >= 128 como octal de maximo 3 digitos (`c.toString(8).padStart(3,'0')`); para chars >= 256 (CJK, emoji, simbolos fuera de WinAnsi como ℃/ﬁ/TM) el octal tiene 4+ digitos y el lector PDF interpreta solo los primeros 3, corrompiendo el resto del string | Cycle 148: `pdfString` emite octal `\ddd` valido para 0x80-0xFF; re-mapea chars >= 0x100 con codigo cp1252 a su byte winansi (tabla `WINANSI_HIGH_CP`: Euro->0x80, TM->0x99, comillas/guiones/puntos) y degrada fuera de winansi (CJK/emoji) a espacio no destructivo; jamas emite octal de 4+ digitos. 9 checks nuevos en `workflow-document-pdf` (75/75) validando en el stream real: sin `/\d{4,}/`, Euro->`\200`, TM->`\231`, CJK/emoji no corrompen. Suite + release gate OK. Commit 6a3a49f. |
| CE-085 | P2 | DONE | Producto/Workspace | La busqueda universal (Ctrl+K, palette) solo filtra navegacion y las 167 herramientas; nunca busca el contenido del usuario (documentos, tablas, capturas). `filterPalette` construye su lista solo con `navItems` + `TOOLS_DATA` (workspace.js:8050-8072); las tres vistas de lista (documentos, datos, capturas) no tienen input de busqueda | Cycle 150: `filterPalette` ahora agrega al proyecto actual los documentos (`navigateTo('doc-editor', {doc})`), tablas (`navigateTo('data-table', {dataTable})`) y capturas (`navigateTo('capture')`) matcheando por nombre y etiquetando el tipo (Documento/Tabla/Captura). `workflow-lifecycle-test` 89/89 (7 checks CE-085 nuevos) y release gate OK. Commit `4f86584`. Limitacion documentada: busca por titulo, no por contenido OCR. |
| CE-086 | P2 | DONE | Producto/Workspace | No hay rename/duplicate/mover para proyectos, documentos, tablas ni capturas: `updateProject` (storage.js:41-54) existe pero la UI nunca lo invoca (importado en workspace.js:8, sin uso); las tarjetas de proyecto/documento/tabla/captura solo ofrecen «Eliminar» (+Encadenar/Extraer/Charts) | Cycle 151: renovar en sitio con dialogo reutilizable `renameEntityModal` (input prefilled con `.ws-input`, valida no-vacio, Enter=confirmar). Botones «Renombrar» en las 4 tarjetas: proyecto→`updateProject(id,{name})`, documento→`saveDoc` (nombre+title), tabla→`saveData`, captura→`saveCapture`. Cada uno refresca el estado y re-renderiza. `workflow-lifecycle-test` 96/96 (7 checks CE-086) y release gate OK. Commit `20cb369`. Limitacion: solo rename en sitio; duplicate/mover quedan como via futura (fila CE-086 cierra rename, no el resto). |
| CE-087 | P3 | DONE | UX/PDF | La vista previa del builder de diseno (WYSIWYG) no coincide con la paginacion del PDF exportado: `estimateSectionHeight` (design-report.js:88) usa altura fija por fila de tabla, mientras el generador real (pdf-generator.js:43-50) calcula altura variable por contenido; imagenes se encajan a 200px en preview (workspace.js:8446) pero a pagina casi completa en el exportado | Cycle 153: `pdf-generator.js` extrae `estimateSectionH`/`estimateTextSectionH` a top-level (reusando `wrapText`, `fitImageDisplay`, `tableColWidth`, `tableRowHeight`) y los exporta. `design-report.js` los importa: la paginacion de la preview decide los saltos en pt con LA MISMA funcion que el PDF (conversion preview px <-> pt, 1mm = 2.835 pt), y `estimateSectionHeight` delega en el mismo estimador. Suite nueva `design-report-wysiwyg-test` 14/14 (pure): la preview pagina identico a una referencia en pt y al numero de paginas del PDF exportado. Release gate OK. Commit `41d602d`. Limitacion: la preview pinta cada seccion con su altura CSS natural (los saltos/alturas estimadas coinciden con el PDF, pero la altura visual del DOM de una imagen puede no igualar el px exacto del exportado). |
| CE-088 | P2 | DONE | Fiabilidad/Workspace | No se puede cancelar el OCR: el boton «Cancelar» de `extractTextFromScan` solo llama `closeModal()` (workspace.js:2640-2642); el worker Tesseract sigue y al resolver crea el documento igualmente. `recognizeText` ya soporta un signal abortable (ocr-engine.js:56-63) que jamas se usa | Cycle 149: `extractTextFromScan` declara `cancelOcr={cancelled:false}`, el boton lo muta y cierra, se pasa como `signal: cancelOcr` a `recognizeText`, y tras recognize si `ocrResult.cancelled` se regresa sin crear documento ni abrir el chooser ni registrar ejecucion. 5 checks nuevos en `workflow-lifecycle-test` (82/82); star-flow E2E OCR real 85/85; release gate OK. Commit dc8c1fb. |
| CE-089 | P3 | DONE | Flujo estrella | El parser de factura asume una sola factura por escaneo: `valueFromLines` (invoice.js:92-103) toma el PRIMER match de cada campo sobre todas las lineas, por lo que un escaneo con 2-3 recibos pierde todos menos uno sin aviso; tampoco detecta limites de factura | Cycle 152: `detectMultipleInvoices` (invoice.js) cuenta lineas de «total final» (Total/Importe total/Amount due — NO subtotal ni IVA) y numeros de factura repetidos; con 2 o mas emite `multipleInvoices {detected,count,note}`. `text.invoice-fields` lo expone como `warning` y el panel del flujo lo muestra; al «Anadir al Workspace» una tabla con warning persiste una fila de aviso y avisa con toast. Con una factura unica NO dispara falso positivo (check dedicado). `invoice-fields-test` 42/42 (7 checks CE-089) y release gate OK. Commit `5f27126`. Limitacion: no divide en facturas separadas (complejidad OCR real); detecta y avisa. |
| CE-090 | P1 | DONE | Persistencia | Data-loss en micro-ventana al cambiar de documento dentro de la vista `doc-editor` (o de tabla en `data-table`) sin navegar: el click de tarjeta hace `appStore.set({ currentDoc: doc, currentView: 'doc-editor' })` (workspace.js:3121) en UNA llamada; si `currentView` ya era `doc-editor`, el store no notifica (state.js:11 comparacion por referencia) -> no corre `renderView` -> no corre `_flushDirtyEntity` (1156-1178) para el doc saliente. El intervalo de autosave (845-873) solo lee el `currentDoc` NUEVO, dejando ediciones del doc saliente sin guardar (hasta 5 s; se pierden si el intervalo dispara antes de que el debounce por-edicion del doc viejo salve). Mismo problema al cambiar de tabla en `data-table` | Cycle 155 (fix): flush-on-switch centralizado y testable. Se extrajo `installEntitySwitchFlush(store)` (subscribers de `currentDoc`/`currentDataTable`) que flushea el SALIENTE en el momento del cambio si su debounce sigue armado (`autoSaveDoc._timer`/`autoSaveTable._timer` != null), mas `_flushOutgoingEntity(entity, kind)` que limpia el timer y guarda ESA entidad por su lock (doc => saveDoc, table => saveData + syncDerivedCharts); `installEntitySwitchFlush(appStore)` se llama en el init junto a los demas subscribers. El gate `_timer != null` evita writes espurios al abrir un doc no editado (el camino normal ya flushea y limpia el timer del saliente en el cambio de vista). Suite nueva `tests/workspace/doc-table-switch-flush-test.mjs` 9/9 (pure, appStore real + capa fiel a storage.js + timers manuales; extrae las 7 funciones reales): escenario 1 doc->doc misma vista persiste el saliente; escenario 2 tabla->tabla misma vista persiste T1 y no toca T2; escenario 3 CONTROL NEGATIVO demuestra que SIN el subscriber la edicion del saliente NO queda persistida al instante (perdida real, el hueco existe); escenario 4 regresion: el cambio de vista con `_flushDirtyEntity` sigue flusheando. Registrada en el release gate. `doc-table-switch-flush` 9/9, CE-082 23/23, cross-entity 55/55, release gate completo OK. |
| CE-091 | P1 | DONE | Workspace | Undo/redo del topbar corruptor en la lista de documentos y desalineado con la edicion de tablas: `_captureWorkspaceState` (workspace.js:830) guarda cada documento de la lista como `{id,name,title,blocks:[{id,content}]}` SIN `block.type`/`html`/`headers`/`rows`; al deshacer el topbar (`_appHistory`, 572) `_applyState` restaura la lista truncada y abrir ese documento dispara `block.type.startsWith` (workspace.js:3804) -> TypeError y crash del editor. Ademas el topbar undo usa el historial GLOBAL mientras las ediciones de celda de tabla usan `tableHistories` (WeakMap) -> el boton Deshacer NO deshace la celda de tabla (lo hace solo Ctrl+Z en tabla) | Cycle 156 (fix): `_captureWorkspaceState` ahora clona los bloques COMPLETOS (`JSON.parse(JSON.stringify(d.blocks))`) y las tablas con filas/sheets/reviewStatus completos (bajo la misma estrategia de `currentDoc`/`currentDataTable` que ya eran clones profundos), retirando el crash de `renderBlock` (redirect `block.type.startsWith` -> `TypeError`). Alineacion con tablas: el Deshacer/Rehacer del TOPBAR y el Ctrl+Z/Ctrl+Y global, cuando la vista es `data-table`, delegan a `undoTableEdit`/`redoTableEdit` (tabla-local, `tableHistories`) en lugar del historial global; fuera de data-table se mantiene `_appHistory`. Suite nueva `tests/workspace/undo-corruption-test.mjs` 15/15 (pure): (A) el snapshot conserva type y campos extra de bloques (html/headers/rows) y clona profundo (retira el crash), y las dataTables conservan filas/sheets/reviewStatus como copias independientes; (B) undo/redo real de tabla deshace la celda y rehace, el historial QEDa separado del global (topbar delegado). Registrada en el release gate. `undo-corruption` 15/15, regresiones de persistencia (CE-082 23/23, storage CE-059/60/61) OK, release gate completo OK. |
| CE-092 | P2 | DONE | Plataforma/recovery | Corrupcion de `localStorage` rompe el arranque completo del Workspace: `state.js:71-72` hace `JSON.parse(localStorage.getItem('toolisto-recent-tools')||'[]')` (y favorite-tools) a nivel de import SIN try/catch. Una sola clave corrupta (escritura parcial, otro tab, edicion manual) lanza al instanciar el modulo `appStore`, antes de `initApp`, dejando pantalla en blanco sin ruta de recuperacion | Cycle 157 (fix): `state.js` ahora usa un helper `readJsonList(key)` que lee de forma segura (try/catch con default `[]` ante JSON invalido O valor no-array) y ELIMINA la clave corrupta (`localStorage.removeItem`) para auto-recuperarse en el siguiente arranque; `recentTools`/`favoriteTools` se inicializan con el helper. A diferencia de los guards `(doc.blocks||[])` esto protege el IMPORT del modulo antes de `initApp`, retirando el crash de boot por cualquier clave de preferencia. Suite nueva `tests/workspace/boot-recovery-test.mjs` 10/10 (pure, carga el CODIGO REAL de state.js con un localStorage controlado): (reproduccion) el parse sin try/catch LANZA con JSON invalido -> era el crash; con el fix el store se construye sin crashear, recentTools/favoriteTools quedan en `[]`, las claves corruptas se eliminan, una clave valida se conserva intacta (sin perdida), y un objeto en lugar de array tambien queda en `[]`. Registrada en el release gate. `boot-recovery` 10/10, release gate completo OK. |
| CE-093 | P2 | DONE | Integridad/importacion | Import de proyecto no valida que las referencias cruzadas remapeadas apunten a entidades existentes en el bundle: `importProject` (storage.js:293-418) remapea `sourceAssetId`/`derivedIds`/`config.sourceTableId`/etc via `remapRefs` (309-350); si el id referenciado no esta en el bundle, `allIdMap.has(id)` es falso y se deja el id original -> referencia colgante persistida. `assertIntegrity` (417) corre DESPUES del commit (fire-and-forget) y solo emite un evento, nunca lo ve el usuario | Cycle 158 (fix): `core/bundle.js` añade `validateBundleReferences(bundle)`, que audita las referencias cruzadas del bundle PRE-COMMIT recalculando el conjunto de IDs importados por tipo de store (documents/dataTables/captures/assets/executions/workflows) + conjunto global, y recorre cada objeto con la MISMA semantica de campos que core/integrity.js (REF_SOURCE_FIELDS, REF_CONFIG_FIELDS, metadata.captureId, relations, inputAssetIds, derivedIds). Rechaza cualquier referencia cuyo destino no exista entre las entidades del bundle; las restringidas por tipo (sourceTableId/tableId->tabla, sourceDocId/scanDocId->documento, captureId ->captura) se validan contra su store correcto. `validateBundleImport` lo ejecuta despues del manifiesto y ANTES de cualquier escritura. Suite nueva `tests/workspace/bundle-reference-validation-test.mjs` 14/14 (pure; carga el CODIGO REAL de bundle.js en un sandbox): bundle integro ok; sourceDocId colgante rechazado; sourceTableId a tabla inexistente rechazado (tipo); metadata.captureId colgante rechazado; relation.targetId colgante rechazado; inputAssetIds colgante rechazado; asset->asset presente ok (se conserva); bundle heredado (sin manifest) con ref colgante igual rechazado; regresion del bundle de referencia importa ok. Registrada en el release gate (Phase 5 y round-trips de storage siguen 100%, sin falsos rechazos). `bundle-reference-validation` 14/14, release gate completo OK. |
| CE-094 | P2 | DONE | Importacion/exportacion | `exportProject` (storage.js:272-291) lee el bundle desde IndexedDB sin flushear los autosaves pendientes en memoria, por lo que una edicion recien hecha dentro de la ventana del debounce se exporta con el valor ANTERIOR. El usuario edita una celda/doc y pulsa «Exportar proyecto» de inmediato: el .toolisto lleva el estado viejo (< 1 s) | Cycle 159 (fix): `workspace.js` añade `_flushDirtyBeforeExport()` (awaitable, por el lock por-entidad) y `exportProjectData` lo invoca ANTES de `exportProject`: si la entidad actual sucia es un doc la guarda por `saveDoc`, si es una tabla por `saveData` + `syncDerivedCharts`; tras el await el bundle se lee con la ULTIMA edicion. `exportProjectFile` delega en `exportProjectData`, por lo que toda ruta de export queda cubierta. Suite nueva `tests/workspace/export-flush-fidelity-test.mjs` 9/9 (pure; `_flushDirtyBeforeExport` REAL de workspace.js + appStore real + capa persistente fiel + reload): doc editado dentro del debounce -> al exportar se persiste la ultima edicion (control negativo: la base aun tenia el valor anterior antes del flush); tabla editada -> se persiste la celda y se invoca syncDerivedCharts; proyecto NO sucio -> sin escritura y sin timers residuales; sin proyecto -> no crashea. Registrada en el release gate. Complementaria de CE-090 (allí navegacion, aqui export). `export-flush-fidelity` 9/9, release gate completo OK. |
| CE-095 | P3 | DONE | Rendimiento/tabla | Historias de deshacer de tabla sin limite de memoria: `commitTableEdit` (workspace.js:4706-4712) empuja un snapshot completo de headers+rows (`snapshotDataTable`) sin cap de `history.past`; ediciones rapidas (Tab por celda dispara `commitTableEdit` en cada blur, 4847-4854) generan N snapshots profundos -> crecimiento lineal de memoria en tablas grandes | Cycle 160 (fix): `workspace.js` introduce `TABLE_HISTORY_LIMIT = 50` (igualando `_appHistory.maxEntries`) y `commitTableEdit` descarta las entradas mas antiguas (`history.past.shift()`) al exceder el limite, conservando el undo reciente. Suite nueva `tests/workspace/table-history-cap-test.mjs` 8/8 (pure; commitTableEdit/undoTableEdit/tableHistories REALES de workspace.js): tras 3x ediciones distinctas history.past no supera 50; la ultima edicion se conserva en el tope; las mas antiguas se descartan; el undo sigue siendo correcto DESPUES de activarse el cap; un commit sin cambio real no apila (dedup); una tabla de 500 filas con 2x ediciones mantiene el historial acotado. Registrada en el release gate. `table-history-cap` 8/8, release gate completo OK. BONUS (mismo ciclo, endurecimiento): la paleta de comandos leia `ws-favorites`/`ws-recent` a nivel de import SIN try/catch (misma clase de crash de boot que CE-092); reutiliza el helper seguro `readJsonList` (ahora exportado de state.js) y envuelve las escrituras en try/catch. Suite `tests/workspace/palette-localstorage-recovery-test.mjs` 12/12. Nota: la descripcion previa del footer (localStorage de favoritos) era una invencion del agente; la entrada REAL de la cola es el cap de tableHistories, que es lo que se entrego. |
| CE-096 | P2 | DONE | Persistencia/sesiones | `cleanupSessionsForProject` (workspace-storage.js:134-166) nunca eliminaba las entidades borradas de los snapshots de `ws:session`: construia `idSet` con las IDs (strings) y filtraba con `!idSet.has(d)` donde `d` es el OBJETO completo (`documents`/`dataTables`/`captures` guardan objetos, no IDs). `Set.prototype.has` usa `===`, asi que `Set.has({id:'x'})` siempre es `false` -> el filtro era un no-op silencioso. Tras borrar un proyecto, las doc/tabla/captura borradas seguian referenciadas en `ws:session` y reaparecian en la recuperacion de sesion (stale data/leak). | Cycle 161 (BUG_FIX, descubierto por DISCOVERY independiente): `cleanupSessionsForProject` compara ahora `d.id` cuando `d` es objeto (`d && typeof d === 'object' ? d.id : d`), con fallback al string por si una sesion guarda IDs crudas. Suite nueva `tests/workspace/session-cleanup-test.mjs` 12/12 (pure; `_loadAllSessions` + `cleanupSessionsForProject` REALES extraidos de workspace-storage.js, dbGet/dbPut stub con envelope): entidades borradas salen de documents/dataTables/captures y las conservadas permanecen; el barrido es global (una entidad borrada no queda referenciada en ninguna sesion); el proyecto borrado nullifica su `currentProjectId` y los no borrados lo conservan; sin sesiones no lanza ni escribe. IMPORTANTE: la suite previa `id-collision-serialization` usaba una SIMULACION autocorregida (`simCleanupForProject` con `d?.id ?? d`), que enmascaraba el bug; esta suite usa el codigo real. Registrada en el release gate. `session-cleanup` 12/12, release gate completo OK. |
| CE-097 | P2 | DONE | Undo/Flujo | `_captureWorkspaceState` (workspace.js:834-847) clonaba TODOS los campos del snapshot menos `flowNodes`/`flowEdges`, que se pasaban POR REFERENCIA (`s.flowNodes || []`). Como el editor de Flow muta las mismas instancias (`renderFlowView`/`scheduleFlowSave` hacen `appStore.set({ flowNodes: nodes, flowEdges: edges })` con la misma referencia), el snapshot de historial y el estado vivo compartian identidad de array/objeto: el undo de Flow restauraba estado ya mutado (historial aliaseado/corrompido), incoherente con los demas campos que CE-091 cubrio con clon profundo. | Cycle 162 (BUG_FIX, candidato #2 del DISCOVERY de Cycle 161): `flowNodes`/`flowEdges` ahora se clonan con `JSON.parse(JSON.stringify(...))` cuando existen (default `[]`). Suite nueva `tests/workspace/flow-snapshot-aliasing-test.mjs` 12/12 (pure; `_captureWorkspaceState` REAL de workspace.js + `createStore` real de state.js): el snapshot.flowNodes/flowEdges NO comparte referencia con el estado vivo; mutar el nodo/edge del estado DESPUES de capturar no cambia el snapshot (x, label anidado, source conservados); sin nodes/edges retorna `[]`; una recaptura tras mutar el nodo refleja el cambio nuevo mientras la captura antigua no se contamina; s1 y s2 no comparten nodos. Registrada en el release gate. `flow-snapshot-aliasing` 12/12, release gate completo OK. Nota: en un primer paso del gate, la suite ajena CE-060 (`stale-delete-lifecycle`) dio un FAIL transitorio (exit 1) por timing; al re-ejecutar el mismo gate quedo RELEASE GATE: OK y la suite pasa 120/120 en aislamiento. Investigacion/degradacion de ese flake queda como candidato futuro (no relacionado con este cambio). |
| CE-098 | P2 | DONE | Correctitud/editor | El `drop` de bloques del documento (workspace.js:4035-4045) hacía `const from = parseInt(e.dataTransfer.getData('text/plain'))` y, si `from !== to`, ejecutaba `doc.blocks.splice(from, 1)` + `splice(to, 0, moved)` + `autoSaveDoc` SIN validar el indice. Un drop extraneo (archivo del SO o seleccion sin nuestro indice) produce `from = NaN`; como `NaN !== to` es `true`, `splice(NaN, 1)` se coerciona a `splice(0, 1)` -> elimina el PRIMER bloque y lo reinserta en `to`, reordenando y autoguardando el documento silenciosamente. | Cycle 163 (BUG_FIX, candidato #5 del DISCOVERY de Cycle 161): se extrae `reorderBlock(blocks, from, to)` (funcion nombrada en workspace.js) que devuelve `false` (no toca el array) si los indices no son enteros en `[0, blocks.length)`, si `from === to`, o si el array esta vacio/no es array; solo reordena y devuelve `true` en un reorder valido. El handler de `drop` llama a `reorderBlock` y solo hace `renderBlocks()` + `autoSaveDoc` cuando devuelve `true`. Suite nueva `tests/workspace/block-drop-reorder-test.mjs` 14/14 (pure; `reorderBlock` REAL extraido): from=NaN/-1/>=length y to fuera de rango devuelven `false` y NO mutan el orden; from==to no-op; reorder valido mueve el bloque correctamente (inicio->final, final->inicio); array vacio/no-array devuelven `false` sin lanzar. Registrada en el release gate. `block-drop-reorder` 14/14, release gate completo OK (42 PASS, 0 FAIL). |
| CE-099 | P3 | DONE | Robustez/memoria | `showBlockMenu` (workspace.js:4061-4093) creaba en cada apertura un `closeMenu` que se registraba en `document.addEventListener('click', closeMenu)` via `setTimeout(0)` y SOLO se auto-eliminaba cuando el click caia FUERA del menu (`!menu.contains(e.target)`). Al hacer click en un ITEM del menu (cuyo onClick hace `menu.remove()`), el click esta DENTRO del menu -> el closeMenu queda colgado en `document` para toda la vida de la pagina. Cada apertura anexaba un listener stale (cerrando sobre el DOM retirado y el closure de renderBlocks/doc). Leak de listeners en sesiones locales largas (la app esta disenada para uso continuo). | Cycle 164 (BUG_FIX, candidato #3 del DISCOVERY de Cycle 161): `showBlockMenu` usa ahora un cierre unico `closeMenu()` que desengancha SIEMPRE el listener (`if (attached) document.removeEventListener('click', onDocClick)`) en TODAS las vias de cierre (seleccionar item, alcanzar limite, click fuera). El attach diferido a macrotask conserva la semantica de que el click que ABRIO el menu no lo cierre; si el menu se cierra antes del timer, el flag `closed` evita enganchar el listener (sin depender de clearTimeout). Suite nueva `tests/workspace/block-menu-listener-test.mjs` 13/13 (pure; `showBlockMenu` REAL de workspace.js + stub de DOM/document/h/svgIcon/config/toast/generateId): seleccionar un item agrega el bloque y desengancha (remove==add); click fuera tambien desengancha; 5 aperturas NO acumulan listeners (add==remove==5, sin listener vivo); limite alcanzado avisa y desengancha; no queda listener vivo en document. Registrada en el release gate. `block-menu-listener` 13/13, release gate completo OK (42 PASS, 0 FAIL). NOTA (regresion honesta): la primera version de este fix usaba `clearTimeout(attachTimer)` en el cierre, lo que desequilibro la verificacion estatica de CE-058 (`setTimeout` vs `clearTimeout`: paso de diff -2 a -3, superando el margen +-2) y rompio `persistence-lifecycle-audit` (94/1). Se corregio reemplazando el clearTimeout por el flag `closed` (el callback del timer no engancha si ya se cerro), restaurando el equilibrio (diff -2) y CE-058 a 95/95; el gate quedo todo OK. Leccion: al anadir setTimeout/clearTimeout globalmente debe respetarse ese balance. |
| CE-100 | P2 | DONE | Rendimiento/memoria | Cada `_appHistory.push/undo/redo` del workspace pasaba un snapshot ya profundamente aislado (`_captureWorkspaceState` clona todos los campos: docs/blocks, tablas/rows/sheets, designConfig, currentDoc/DataTable, flowNodes/Edges con JSON.parse(JSON.stringify)), pero `cloneState` configurado en `createHistoryManager` hacia `JSON.parse(JSON.stringify(s))` OTRA VEZ (workspace.js:1030 + history-manager.js:16). Eso serializaba el workspace COMPLETO dos veces por cada push/undo/redo y duplicaba la memoria en el historial de 50 entradas (perf + memoria en workspaces locales grandes). | Cycle 165 (PERFORMANCE_IMPROVEMENT, candidato #4 del DISCOVERY de Cycle 161): `cloneState` pasa a identidad `(s) => s` porque TODAS las llamadas a `_appHistory.{push,undo,redo}` pasan `_captureWorkspaceState()` (verify: solo esos 3 usos existen, pushGrouped no se usa), que ya devuelve un snapshot aislado; re-clonarlo era redundante. Suite nueva `tests/workspace/undo-clone-once-test.mjs` 7/7 (pure; `createHistoryManager` REAL + `_captureWorkspaceState` REAL + contador instrumentado de JSON.parse/stringify): push con cloneState=identidad hace 0 serializaciones internas (antes 1 JSON completo por llamada); undo devuelve snapshots NO referenciados al estado vivo; la captura aislada por `_captureWorkspaceState` se mantiene aislada en el historial (x no se contamina al mutar el vivo); workspace.js declara cloneState identidad. Registrada en el release gate. `undo-clone-once` 7/7, release gate completo OK (42 PASS, 0 FAIL). CE-091 (undo-corruption) sigue pasando, confirmando que la semantica de undo/redo no cambio. |
| CE-101 | P1 | DONE | Correccion de exportacion | `exportTableCSV` (workspace.js) construia la fila de headers con `table.headers.join(',')` SIN ESCAPAR (un header con coma/comillas corrompe la estructura CSV exportada) y aplicaba `c.includes(...)` sobre el valor DE CELDA en bruto (un `null`/`undefined`/numero/booleano lanzaba `TypeError: c.includes is not a function`). Su funcion hermana `queryExportCsv` ya tenia el escape correcto (`String(value == null ? '' : value)` + `/[,"\n]/`), asi que `exportTableCSV` quedaba inconsistente y mas fragil. | Cycle 166 (CE-101, DISCOVERY 2da ronda): `exportTableCSV` refactorizado para usar el MISMO escape que `queryExportCsv`: coerción a string, escaping de comas/comillas/salto de linea en headers Y celdas, y linea de datos nueva. Suite `tests/workspace/table-csv-escape-test.mjs` 9/9 (pure; `exportTableCSV` REAL + `queryExportCsv` REAL + DOM/Blob/URL stub): header con coma `"Precio, USD"` queda entre comillas, header con comillas se duplica `"""Nomina"""`, el CSV conserva el BOM `\uFEFF`, filas de datos intactas, celdas null/undefined/numero/booleano YA no lanzan y se coaccionan, y el escape es BYTE-IDENTICO a `queryExportCsv` (solo difiere el BOM). Registrada en el release gate. |
| CE-102 | P1 | DONE | Correccion de query/detect-type | En `queryRunOperation` (workspace.js), el transform `detect-type` normalizaba una fecha con `new Date(value).toISOString().slice(0, 10)` (linea ~6014). Para fechas con barra (`MM/DD/YYYY`, `YYYY/MM/DD`) `Date.parse` las interpreta como MEDIANOCHE LOCAL, pero `toISOString()` emite UTC: en zonas con offset != 0 la fecha calierta cambiaba (detalmente en el limite de dia), y para `YYYY-MM-DD` con offset positivo podia desplazarse hacia adelante. Resultado: corrompia silenciosamente las fechas de las celdas durante detect-type segun la zona horaria del usuario, en vez de conservar la fecha escrita. | Cycle 167 (CE-102, DISCOVERY 2da ronda candidato #2): nuevo helper `queryDateToIso(value)` que reconstruye la fecha desde sus COMPONENTES LOCALES (sin pasar por UTC): si el anio esta al inicio usa anio/mes/dia; si esta al final usa mes/dia/anio (semantica US que ya tenia Date.parse), validando rangos y rellenando a dos digitos; solo como fallback usa `new Date(...).toISOString().slice(0,10)` para valores no captados por la regex. `detect-type` ahora llama a `queryDateToIso`. Suite `tests/workspace/query-date-to-iso-test.mjs` 11/11 (pure; `queryDateToIso` REAL): YYYY-MM-DD, YYYY/MM/DD, MM/DD/YYYY y padding se conservan; salida == fecha escrita en todos los formatos; salida == fecha CALENDARIO LOCAL (invariante TZ deterministica respecto a getFullYear/getMonth/getDate); detect-type usa queryDateToIso y ya no usa toISOString. Registrada en el gate. |
| CE-103 | P1 | DONE | Correccion de formulas | En `parseTerm` (dentro de `safeArithmetic`, workspace.js:4720), la division por cero se forzaba silenciosamente a `0`: `value = operator === '*' ? value * right : (right === 0 ? 0 : value / right)`. Asi una formula `=A1/0` (o `=SUM(...)/0`) escribia `0` en la celda en lugar de indicar el error; un valor de hoja de calculo silenciosamente incorrecto. El contrato de errores del codigo ya era `#FORMULA` (cuando `safeArithmetic` devuelve vacio), asi que `0` era un resultado erroneo no señalado. | Cycle 168 (CE-103, DISCOVERY 2da ronda candidato #3): en `parseTerm` la division por cero devuelve `NaN` en lugar de `0`. Como `undefined`/`Infinity`/`NaN` no son finitos, el guard final de `safeArithmetic` (`Number.isFinite(result) ? result : ''`, linea ~4738) lo rechaza y `evaluateDataFormula` lo mapea a `#FORMULA` (error visible), coherente con `=abc` y `=1/()`. Suite `tests/workspace/div-by-zero-formula-test.mjs` 12/12 (pure; `safeArithmetic` REAL + `evaluateDataFormula` REAL + helpers REALES `parseLocaleNumber`, `columnNameToIndex`, `indexToColumnName`, `cellReferenceToPosition`, `numericValue`): nivel nucleo `safeArithmetic('5/0')==''`, `0/0==''`, `10/(2-2)==''`, `5/2==2.5`, `0/5==0`, `2+3*4==14`; contrato final `=A1/0 -> #FORMULA`, `=B2/0 (referencia a formula B1/0) -> #FORMULA`, `=5/2 -> 2.5`, `=0/5 -> 0`, `=A1+A2 (5+3) -> 8`; y `parseTerm` divide por cero con `NaN` (no `0`). Registrada en el gate. |
| CE-104 | P2 | DONE | Robustez de imagen | `createThumbnail` (workspace/core/image-processor.js:53) calculaba `scale = Math.min(maxSize / w, maxSize / h, 1)` y luego `canvas.width = Math.round(w * scale)`. Con una imagen degradada con `w` o `h` en 0 (o NaN/Infinity), `maxSize / 0 = Infinity`, `Math.min(...,1)` quedaba en 1 y el canvas resultante era `0xN`, `Nx0` o `0x0`: una miniatura en blanco cultivada en IndexedDB sin error (perdida silenciosa). `processImageCapture` (linea ~560) llamaba `thumbnail.toDataURL('image/jpeg',0.85)` incondicionalmente sobre ese canvas vacio. | Cycle 169 (CE-104, DISCOVERY 2da ronda candidato #4): `createThumbnail` retorna `null` si `w`/`h` no son finitos o `<= 0`, y clampea las dimensiones chichuelas a `>=1` para fuentes validas (protege contra fracionarios). `processImageCapture` guarda el thumbnail null con un ternario (`thumbnail ? thumbnail.toDataURL('image/jpeg',0.85) : ''`), sin crashear y sin guardar un asset 0x0. Suite `tests/workspace/thumbnail-guard-test.mjs` 12/12 (pure; `createThumbnail` REAL + `processImageCapture` REAL + document stub): imagen valida 800x600 -> miniatura <=400 preservando 4:3; `w=0`, `h=0`, `w=0/h=0`, `w=NaN`, `h=Infinity` -> `null`; 2x3 valida -> canvas valido; processImageCapture usa el ternario de guarda. Registrada en el gate. |
| CE-105 | P1 | DONE | Correccion de query/rebuild | En `queryRunOperation` (workspace.js:5926-5941), las operaciones `choose-columns` y `reorder-columns` mapeaban los indices SIN validar rango: `result.headers = chosen.map(column => headers[column])` y `rows.map(row => order.map(column => normalize(row[column])))`. `queryRebuildModel` (6108-6116) re-ejecuta TODOS los pasos almacenados en cadena desde `baseHeaders`. Un paso previo (`remove-columns`) puede reducir la cantidad de columnas, dejando los indices construidos de pasos posteriores fuera de rango: `headers[column]` -> `undefined` y `normalize(row[undefined])` -> `''` -> perdida/silencio silencioso de datos SIN error visible. | Cycle 170 (CE-105, DISCOVERY 3ra ronda candidato #1): en `remove-columns`/`choose-columns` y `reorder-columns` se filtran `indexes` a `Number.isInteger(col) && col >= 0 && col < headers.length` antes de construir `chosen`/`order`, de modo que los indices fuera de rango se DESCARTAN (nunca `headers[col]` es `undefined`) y el modelo queda intacto cuando todos quedan fuera de rango (`if (!chosen.length) return result`). Suite `tests/workspace/query-column-range-guard-test.mjs` 16/16. Registrada en el gate. |
| CE-106 | P2 | DONE | Correccion de exportacion | Inconsistencia entre hermanas (patron CE-101): tanto `exportDocument` (workspace.js:4145) como `blocksToMarkdown` (workflow-operations.js:694) escapaban el pipe `|` en las CELDAS de las tablas Markdown (`replace(/\|/g,'\\|')`) pero NO en los ENCABEZADOS (`headers.map(String).join(' | ')`). Un header con `|` (p. ej. `Precio | IVA`) rompia la estructura de la tabla Markdown exportada (4 columnas en la cabecera frente a 2 en las filas); su hermana HTML (`documentBlocksToHtml`) si escapa. | Cycle 171 (CE-106, DISCOVERY): ambos paths escapan ahora el pipe en los encabezados con la MISMA coercion y escape que las filas (`headersT.map(v => String(v ?? '').replace(/\|/g,'\\|'))` / `block.headers.map(...)`). Suite nueva `tests/workspace/md-header-escape-and-replace-test.mjs` (12 checks: header con pipe en ruta de flujo, header sin pipe regresion identica, replace-values guard). Registrada en el release gate. Regresiones: `workflow-export-md` 30/30, `text-to-document` 15/15, `query-column-range-guard` 16/16, `query-date-to-iso` 11/11. Commit `a9a971b`. |
| CE-108 | P3 | DONE | Correccion de query | `replace-values` en modo `contains` con `find` vacio corrompia la columna entera: `current.split(String(config.find)).join(String(config.replace))` con `find=''` hacia `split('')`, partiendo cada celda en CARACTERES y uniendo con el reemplazo (`"Factura"` -> `"XFXaXcXtXuXrXaX"`). El modal de la UI (workspace.js:6500) no validaba `find` vacio. Bug de corrupcion de datos silenciosa y sin guard. | Cycle 171 (CE-108, DISCOVERY): guard en la operacion (`findStr === '' ? current : ...` deja la celda intacta en contains) y en el UI (toast `Escribe un texto para buscar` + aborta cuando `mode==='contains' && !config.find`). Test 5 checks en la suite CE-106 (find vacio, contains no vacio regresion, exact, numeros intactos). Registrada en el gate. Commit `a9a971b`. |
| CE-109 | P1 | DONE | Correctitud/formulas | Dos bugs en el motor de formulas de hoja de calculo: (a) `safeArithmetic` (workspace.js:4694-4738) NO parseaba el operador unario menos: su tokenizer separa `-` como token y `parsePrimary` hacia `Number('-')=NaN` devuelto como 0 y avanzaba; cualquier formula que resolviera a (o contuviera) un numero negativo daba `''` -> `#FORMULA` (`=A1+B1` con A1=`-5` -> `#FORMULA` en vez de `-2`; `=MIN(A1:A3)` sobre negativos -> `#FORMULA`; `=5*-3` -> `#FORMULA`). (b) En los agregados (`SUM/AVERAGE/AVG/MIN/MAX/COUNT`), `resolveCell` devuelve `numericValue(raw) ?? 0`, por lo que las celdas NO numericas (`"abc"`) se convertian en `0`, y el filtro posterior (`Number(String(0).replace(',','.'))`) conservaba ese `0`: COUNT/AVERAGE/MIN/MAX contaban/incluian celdas no numericas (`=COUNT(A1:A3)` con abc,100,200 -> 3 en vez de 2; `=MIN` -> 0 en vez de 100; `=MAX` sobre -5,-3 -> 0 en vez de -3). | Cycle 172 (CE-109, DISCOVERY): (a) `parsePrimary` de `safeArithmetic` consume signos unarios `-`/`+` antes del operando (sign flag, aplicado tambien a parentesis), cubriendo `-5`, `-5+3`, `5*-3`, `3*(-1)`, `5--3`, `+7`. (b) nuevo `cellsFromArgument` (reemplaza `valuesFromArgument`) que resuelve cada celda (formulas incluidas) y la parsea con `parseLocaleNumber` retornando un numero o `null`, y los agregados filtran `null`/no-finito: COUNT/AVERAGE/MIN/MAX/SUM ignoran celdas no numericas (semantica de Excel/Sheets) y respetan decimales de miles europeos; `COUNTA` sigue contando no vacias. Suite nueva `tests/workspace/formula-unary-and-aggregates-test.mjs` 27/27 (pure; safeArithmetic/evaluateDataFormula/helpers REALES): unario negativo en el nucleo y en formulas (`=-5+3 -> -2`, `=MIN` negativos), agregados con texto mezclado (COUNT/AVERAGE/MIN/MAX correctos), europeos (`=SUM 4035.31`), todos no numericos -> 0 sin crash, COUNTA intacta, agregado sobre celdas formula, anti-regresion estatica (unario +/- presente, `valuesFromArgument` eliminado, filtro numerico). Registrada en el gate. Regresiones: `div-by-zero-formula` 12/12 (CE-103), `query-date-to-iso` 11/11. RELEASE GATE completo 76 suites PASS (incl. CE-109) 0 fail. |
| CE-110 | P2 | DONE | Correctitud/orden de tablas | Los dos sitios que ORDENAN una tabla (workspace.js sort por columna del modal de ordenamiento y el sort por click en la cabecera) comparaban numeros con el parser AD-HOC `Number(a.replace(',', '.'))` en vez del parser canonico `parseLocaleNumber`. Con formato europeo de miles (`1.234,56`) el `replace(',','.')` producia `1.234.56` -> `Number` = `NaN`, cayendo al `localeCompare` y ORDENANDO COMO TEXTO (`1.200` < `1.3` lexicamente, cuando 1200 > 1.3 numericamente); violaba el contrato del repo «All modules must use [parseLocaleNumber] instead of ad-hoc parsing» (CE-072). | Cycle 173 (CE-110, DISCOVERY): nuevo helper de modulo `compareTableValues(a, b)` (workspace.js:4687) que compara con `parseLocaleNumber` (numero) o con `localeCompare` en es como fallback para no numericos; los DOS call-sites de sort (modal de ordenamiento y click en cabecera) lo usan y se elimina el ad-hoc `Number(replace(',','.'))` (verificado por anti-regresion estatica). Los valores europeos de miles/negativos/decimales con coma ahora ordenan numericamente en ambos puntos de entrada; texto puro y vacios siguen por `localeCompare` sin crash. Suite nueva `tests/workspace/table-sort-locale-test.mjs` 16/16 (compareTableValues + parseLocaleNumber REALES): simples sin regresion, `100/20/3` -> `[3,20,100]`, decimales con coma, millares europeos, negativos asc/desc, texto/vacio sin crash, mezcla, `1.200` vs `1.3` numerico, anti-regresion estatica (helper en 2 sitios, ad-hoc eliminado). Registrada en el release gate. RELEASE GATE completo: 76 suites PASS 0 fail. |
| CE-111 | P2 | DONE | Export Markdown | `exportDocument` (exportar un documento como Markdown, vista Documentos / flujo estrella `documento → informe`) NO emitian las listas ni las imagenes: los bloques `bullet-list`, `numbered-list` e `image-block` caian al fallback generico `content` del texto plano, perdiendo la estructura de lista y vertiendo la `dataUrl` de la imagen como texto plano ilegible. Era asimetrico con la referencia canonica `blocksToMarkdown` (workflow-operations.js:678, que maneja `bullet-list` en 688 y `image-block` en 717) — el MISMO documento exportaba distinto segun la ruta. | Cycle 174 (CE-111, DISCOVERY): se extrajo `exportDocumentMarkdown(doc)` puro (workspace.js:4127-4162) de `exportDocument`, conservando TIODO el comportamiento previo (page-break html, divider, heading1-3, quote, code, callout, tabla) y anadiendo: `bullet-list` -> `- content`, `numbered-list` -> `1. content`, `image-block` -> `![imagen](src)` con `String(block.content || block.dataUrl || '')` (content con precedencia, paridad `blocksToMarkdown`). `exportDocument` ahora delega en `exportDocumentMarkdown(doc)`. Suite nueva `tests/workspace/document-export-md-lists-test.mjs` 23/23 (REAL, pura): viñetas, listas numeradas, imagen por dataUrl y por content, precedencia content, mezcla ordenada, quote/divider/callout/code/tabla intactos, bloque desconocido -> contenido, anti-regresion estatica (exportDocument llama a exportDocumentMarkdown; maneja los 3 tipos nuevos). NOTA de anclaje: la suite CE-106 `md-header-escape-and-replace-test.mjs` anclaba su anti-regresion en `async function exportDocument` y la refactorizacion la reubico en `exportDocumentMarkdown` (definida antes); se actualizo el ancla estatica a `function exportDocumentMarkdown` SIN debilitar la asercion (`replace(/\|/g` sigue verificado) — sigue 12/12. Registrada en el release gate. RELEASE GATE completo: 77 suites PASS 0 fail. |
| CE-112 | P2 | DONE | Integridad/import-export | Un `null` dentro de la matriz `relations` de una entidad crasheaba DOS caminos asimetricos del flujo de datos: (a) IMPORTAR: `importProject` remapea los ids via `remapRefs` (storage.js:333) con `obj.relations.map(r => ({ ...r, ... }))` -> una entrada `null` reventaba `...r` (TypeError) antes de escribir; (b) EXPORTAR: `exportProject` construye el manifiesto via `buildManifest` -> `collectRelations` (bundle.js:201-212) accediendo a `r.targetId || r.to` -> reventaba con `r` null. Ambos eran asimetricos con `collectRefIds` (bundle.js:116) que YA tenia `if (!rel) continue;`, y `validateBundleImport` IGNORA el null (no lo rechaza), asi que el null llegaba vivo a `remapRefs`. | Cycle 175 (CE-112, DISCOVERY 8va ronda): se anade el guard que faltaba en los DOS sitios (paridad con `collectRefIds`): `collectRelations` hace `if (!r) continue;` (salta el null en el conteo/derivacion del manifiesto) y `remapRefs` descarta las entradas null de `relations` (`.map(...).filter(r => r !== null && r !== undefined)`) de modo que una relacion valida se remapea al id nuevo y el null no se persiste. Suite nueva `tests/workspace/relations-null-guard-test.mjs` 14/14 (CODIGO REAL de bundle.js+storage.js en sandbox IDB): IMPORT con `relations: [null, {targetId:'doc-b'}]` no lanza, el null se descarta y el targetId se remapea al id nuevo; EXPORT via `buildManifest` con relation null no lanza y `relationCount=1` (solo la valida); anti-regresion estatica (guard presente en ambos sitios). Registrada en el release gate. RELEASE GATE completo: 78 suites PASS 0 fail. |
| CE-113 | P2 | DONE | Persistencia/navegacion | `_flushDirtyEntity()` decidia QUE entidad flushear leyendo `appStore.get('currentView')`. Pero `renderView` corre a traves del subscriber de `currentView` (workspace.js:995), que se dispara DESPUES de que `navigateTo` (workspace.js:727) ya haya puesto la vista NUEVA. Asi, al salir del editor hacia una vista de tabla (o dashboard/documents) dentro de la ventana del debounce: `_flushDirtyEntity` leia la vista NUEVA y flusheaba la tabla entrante (consumiendo la bandera COMPARTIDA `isDirty` de la edicion del doc) o NO flusheaba nada; `renderView` luego cancelaba el debounce del doc (`clearTimeout(autoSaveDoc._timer)`) y, con `isDirty` ya en false, ni el intervalo de 5s salvaba la ultima edicion del doc saliente -> PERDIDA REAL de datos | Cycle 176 (CE-113, DISCOVERY 9na ronda): se pasa la vista SALIENTE hasta `_flushDirtyEntity`: el subscriber de `currentView` recibe `(view, prevView)` (state.js:12) y lo reenvia a `renderView(view, prevView)`; `renderView` llama `_flushDirtyEntity(prevView)`; `_flushDirtyEntity(outgoingView)` usa `outgoingView || appStore.get('currentView')` (fallback para callers directos como `refreshCurrentView`). Suite nueva `tests/workspace/flush-outgoing-view-test.mjs` 20/20 (CODIGO REAL + appStore real + capa fiel + shim renderView fiel + reload): FIX doc->tabla conserva el doc saliente y no contamina la tabla; CONTROL NEGATIVO sin prevView el doc saliente SE PIERDE (isDirty se consume con la tabla, debounce cancelado); FIX tabla->doc conserva la celda saliente; CONTROL NEGATIVO simetrico; FIX doc->dashboard (vista sin entidad) conserva el doc; compatibilidad `_flushDirtyEntity()` sin prevView cae a currentView; anclas estaticas. Anclas de auditoria reactualizadas sin debilitar: `cross-entity-integrity` 55/55, `persistence-lifecycle-audit` 95/95, `storage-recovery-lifecycle` 69/69. Registrada en el release gate. RELEASE GATE completo: 79 suites PASS 0 fail. |
| CE-114 | P2 | DONE | Persistencia/undo | `_captureWorkspaceState()` (workspace.js:842) snapshotteaba las capturas con `dataUrl: c.dataUrl ? c.dataUrl.slice(0, 200) : null`. Ese snapshot alimenta el historial de undo/redo Y el autosave; al deshacer/rehacer, `_applyState` reescribia el store con `captures: snapshot.captures || []` (dataUrls TRUNCADOS a 200 chars). El resto del codigo usa SIEMPRE el dataUrl completo (`saveImageCapture`, `renderCaptureView`, `extractTextFromScan`, `saveWorkspaceSession` con `captures: appStore.get('captures')`), asi que un solo Ctrl+Z en el editor corrompia en silencio la imagen de TODA captura almacenada (thumbnail roto, OCR fallido) y la corrupcion se persistia en la sesion | Cycle 177 (CE-114, DISCOVERY 10ma ronda): las capturas se EXCLUYEN del historial de undo/redo a proposito (son datos anexo-apendice vivos mantenidos por su propio flujo, NUNCA editados por acciones deshacibles de doc/tabla): `_captureWorkspaceState()` ya no incluye `captures` y `_applyState()` ya no restaura `captures` desde el snapshot. Suite nueva `tests/workspace/capture-history-dataurl-test.mjs` 12/12 (CODIGO REAL `_captureWorkspaceState` + appStore real + historial fiel + CONTROL NEGATIVO que reimplanta el slice(0,200) y prueba que SI corrompe la captura): el snapshot ya no tiene `captures`; la captura viva conserva su dataUrl completo tras un ciclo capture->snapshot->undo->apply; el control negativo pisa el dataUrl truncado (200 chars, roto) demostrando que el fix es necesario; anclas estaticas (no existe slice(0,200) ni restauracion de captures en _applyState). Registrada en el release gate. RELEASE GATE completo: 80 suites PASS 0 fail. |
| CE-115 | P2 | DONE | Persistencia/import | `importProject` (core/storage.js:383-388) remapeaba el id fuente de dashboard y query SOLO en la forma `dashboard.config.sourceId` / `query.config.sourceId`, pero el shape REAL que se persiste y exporta lleva `sourceId` a nivel TOP: el dashboard (`dashboardNormalizeConfig` lee `saved.sourceId` workspace.js:7182 y lo emite top-level en 7199; `dashboardDefaultConfig` en 7167) y la query (`querySerializeModel` top-level workspace.js:5732, `queryCreateModel` 5706; al leer `queryModelFromSaved` hace `tables.find(id === saved.sourceId)` en 5742, con fallback a `baseHeaders/baseRows` embebidos). Como import re-crea cada tabla con un id NUEVO (`tableIdMap`), un round-trip export->import dejaba el `sourceId` original (p. ej. `table-b`) inexistente: dashboard silenciosa a `tables[0]` (panel con datos de la tabla equivocada) y query desconectada de su tabla viva (baseRows congelados) | Cycle 178 (CE-115): `importProject` remapea TAMBIEN el `sourceId` top-level de `dashboard`, `query` y `query.sheets[].sourceId`, conservando el `config.sourceId` defensivo para bundles legacy. Suite nueva `tests/workspace/import-sourceid-remap-test.mjs` 19/19 (CODIGO REAL bundle+storage en sandbox con fake-indexeddb; reset de DB por escenario para determinismo): dashboard.sourceId remapeado al id NUEVO de la fuente correcta (ni `tables[0]` ni el id original), query.sourceId remapeado a la fuente correcta, control negativo (find del sourceId original falla + fallback POSICIONAL a `tables[0]` + query desconectada), compat legacy `config.sourceId` intacta, anclas estaticas. Registrada en el release gate. RELEASE GATE completo: 80+ suites PASS 0 fail. |
| CE-116 | P1 | DONE | Query/detect-type | `queryIsDate` (workspace.js:5837) usaba regex `[-/]` que no reconocia el punto `.` como separador; fechas europeas `DD.MM.YYYY` se clasificaban como `ABC` en vez de `DATE`. Ademas `queryDateToIso` (5842) lanzaba `RangeError` (Invalid time value) al intentar `new Date("23.08.2026").toISOString()` en el fallback, porque `Date.parse` con punto devuelve NaN. Ambas funciones estaban duplicadas respecto a `core/locale-parser.js` (que SI soporta DD.MM.YYYY via `classifyDate`) pero workspace.js no importaba el clasificador | Cycle 179 (CE-116): `queryIsDate` anade branch explicito para DD.MM.YYYY con validacion de rangos (anio 1000-9999, mes 1-12, dia 1-31); `queryDateToIso` anade branch dot-separated ANTES del fallback que crashea, reconstruyendo la fecha desde componentes como hace con slash/barra. Se conserva la regex original `[-/]` para los formatos existentes (sin regresion). Suite existente `tests/workspace/query-date-to-iso-test.mjs` ampliada a 30/30 (nuevos checks: queryIsDate reconoce DD.MM.YYYY y rechaza invalidos, queryDateToIso normaliza DD.MM.YYYY a ISO sin RangeError, conserva formatos previos). RELEASE GATE completo suites PASS 0 fail. |
| CE-117 | P2 | DONE | Integridad/referencias | `validateBundleReferences` (bundle.js) y `allowedStoreForField` (integrity.js) NO validaban los campos `correctedAssetId`, `originalAssetId`, `scanDocumentId` ni `assetId` de las referencias de escáner: el bundle estaba en `REF_SOURCE_FIELDS`/`REF_CONFIG_FIELDS` y el mapa de `allowedStoreForField` solo cubria los campos legacy (`sourceDocId`, `sourceTableId`, `captureId`), asi que un asset con `correctedAssetId` apuntando a un documento o tabla pasaba silencioso al importar; el bug era oculto porque `resultAssetId` y `sourceAssetId` ya estaban en `REF_SOURCE_FIELDS` con mapeo `null` (cualquier tienda) por su uso legitimo en docs/tablas/executions, y añadirlos al caso `'assets'` rompia esos caminos | Cycle 180 (CE-117): los 4 campos especificos del escaneo (`correctedAssetId`, `originalAssetId`, `scanDocumentId`, `assetId`) se anaden a `REF_SOURCE_FIELDS`/`REF_CONFIG_FIELDS` y a `SOURCE_FIELDS`/`CONFIG_FIELDS` (integrity.js), y sus correspondientes `case` en `refAllowedStoreKind`/`allowedStoreForField` retornan `'assets'` (solo assets). `resultAssetId`/`sourceAssetId` permanecen con mapeo `null` (cualquier tienda). Suite nueva `tests/workspace/scanner-refs-in-guards-test.mjs` 11/11: dangling refs rechazadas, valid refs aceptadas, anclas estaticas para ambas listas y mapping. Mirrors `SOURCE_FIELDS`/`CONFIG_FIELDS` actualizados en `storage-recovery-lifecycle`. Registrada en el release gate. RELEASE GATE completo suites PASS 0 fail. Commits `163fd8b` + `6c47aef`. |
| CE-118 | P2 | DONE | Fiabilidad/import | `importQueryFile` (workspace.js) usaba `.map(line => JSON.parse(line))` sobre lineas JSONL/NDJSON: una sola linea malformada lanzaba `SyntaxError` que abortaba la cadena `.map()`, descartando TODOS los registros validos del archivo sin aviso parcial ni recuperacion; el catch externo mostraba un toast generico y el archivo entero se perdia | Cycle 181 (CE-118): nuevo `parseJsonlText(text)` (funcion pura, extraida de importQueryFile) que itera con try/catch por linea, colecciona registros validos y cuenta lineas descartadas; si hay descartes muestra toast de aviso con el conteo; si TODAS las lineas son invalidas muestra toast de error y aborta (sin importar tabla vacia). Suite nueva `tests/workspace/jsonl-import-resilience-test.mjs` 31/31: validos completos, 1 malformada en el medio preserva el resto, todas malformadas, input vacio, whitespace-only, mezcla, trailing comma, objetos anidados, unicode, CRLF, anti-regresion de forma. Registrada en el release gate. RELEASE GATE completo 81 suites PASS 0 fail. Commit `2639134`. |
| CE-119 | P2 | DONE | Workspace/Charts | `tableChartData` (workspace.js) y `tableChartSeries` (workflow-operations.js) default`ean `numericIndex = 1` cuando `headers.length < 2`: si la tabla solo tiene 1 columna, `numericIndex` apunta a `undefined` y `series[].data` extrae la columna de indices, produciendo undefined o NaN; `syncDerivedCharts` re-escrivbia los charts existentes con serie vacia cuando el tableData tenia <2 headers, corrompiendo graficos ya validos | Cycle 182 (CE-119): `tableChartData` retorna `{ series: [], numericIndex: null }` cuando `headers.length < 2`; `tableChartSeries` (workflow-operations.js) retorna `{ series: [], numericIndex: null }` cuando `safeHeaders.length < 2`; `syncDerivedCharts` hace `if (headers.length < 2) return` antes de tocar charts; `chart.create` y `report.create` validan `headers.length < 2`. Suite nueva `tests/workspace/table-chart-oob-guard-test.mjs` 25/25: 1 header, 0 headers, missing headers, null table, 2+ headers (regresion), 3 headers, empty rows, non-numeric, workspace tableChartData guard, syncDerivedCharts guard. Registrada en el release gate. RELEASE GATE completo 82 suites PASS 0 fail. Commit `a4f690d`. |
| CE-120 | P2 | DONE | Fiabilidad/storage | 13 `.catch(() => {})` vacios (y 1 try/catch) en workspace.js + 1 en storage.js tragaban en silencio los fallos de IndexedDB/localStorage. El caso mas grave: `renderCaptureView` (workspace.js:2228) NO tenia el guard `_viewGeneration` (sus hermanas de documentos:3257 y datos:4448 SI) — una lectura lenta de IndexedDB puede resolver DESPUES de navegar y pisar `appStore.captures` con datos stale de una navegacion anterior; peor, `showModal` (`try { await opts.onConfirm(); } catch (e) { return; }`) tragaba TODAS las fallas de almacenamiento de eliminar/renombrar/guardar config sin feedback (el usuario cerraba creyendo que se habia borrado/guardado) | Cycle 183 (CE-120, DISCOVERY 14ta ronda): (1) `renderCaptureView` anade `const viewGeneration = _viewGeneration` + `if (viewGeneration !== _viewGeneration) return;` antes de tocar el store (paridad con docs/data) y su catch reporta `capture-list-load`; (2) `renderDocumentsView` y `renderDataView` pasan de `.catch(() => {})` a `reportError` (`document-list-load`/`data-list-load`); (3) `showModal` reporta via `reportError(..., 'modal-confirm')` y mantiene el modal abierto; (4) `saveWorkspaceConfig` reporta `workspace-config-save` si `localStorage.setItem` lanza; (5) `deleteCapture` (storage.js) reporta `capture-delete-counts` si `refreshProjectCounts` falla. Suite nueva `tests/workspace/swallowed-storage-errors-test.mjs` 24/24 (funciones REALES + behavioral + anclas estaticas): guard de captures presente, catches reportan en los 3 views, modal mantiene abierto y reporta, saveWorkspaceConfig reporta al fallar (y NO reporta al exito con merged config), storage.js importa reportError y deleteCapture lo usa, 0 `.catch(() => {})` restantes en workspace.js (antes 13), claves de contexto unicas. Registrada en el release gate. RELEASE GATE completo 83 suites PASS 0 fail. Commit `1d756e9`. |
| CE-121 | P2 | DONE | Dashboard/graficos | `dashboardChartItems` (workspace.js:7266) usaba `String(row[categoryIndex] || 'Sin categoría')` que colapsa ceros numericos (y falsy values) a la etiqueta default: `String(0 || 'Sin categoría')` = `"Sin categoría"`. En la UI actual el bug estaba enmascarado porque `dashboardVisibleRows` (7239) coacciona todo a `String()` antes de pasarlo a `dashboardChartItems`, asi que `0` llega como `'0'` (truthy). Sin embargo `dashboardChartItems` es una funcion pura con contract publico (exportada inline en el render del dashboard) que podria recibir filas con ceros numericos directamente si se refactoriza el pipeline | Cycle 184 (CE-121): reemplaza el falsy-coalescing `row[categoryIndex] || 'Sin categoría'` con un null-check explícito `const raw = row[categoryIndex]; const category = (raw == null ? '' : String(raw).trim()) || 'Sin categoría';` — preserva `0` (y cualquier valor falsy no-nulo) como label `'0'`, sigue colapsando celdas vacias/nulas a "Sin categoría". Suite nueva `tests/workspace/dashboard-chart-category-zero-test.mjs` 15/15: numeric 0 como label, empty/null/undefined a "Sin categoría", whitespace-only a "Sin categoría", count aggregate con 0, regresion string "0", ancla statica de dashboardVisibleRows y del guard. Registrada en el release gate. RELEASE GATE completo 84 suites PASS 0 fail. Commit `c6d6b08`. |
| CE-122 | P2 | DONE | Limpieza/PDF + Fiabilidad/test | (a) `createPdfBlob` (workspace/core/workflow-operations.js ~946-968) tenia 23 lineas de scaffolding muerto: `header`/`body`/`offsets`/`pos`/`off1..off5`/`lines`/`objects`/`imgObj`/`imgEnd` calculados para un primer `wx.Compose` pero nunca usados en el segundo `wx.Compose` que produce el PDF real. (b) El harness `stale-delete-lifecycle` (CE-060) usaba sleeps fijos `delay(30-80ms)` para esperar el completado de operaciones asincronas: bajo la carga del gate completo los sleeps disparan tarde y el gate fallo 2 veces en secciones distintas (§1.7, §3.9/3.10) pasando 120/120 en aislamiento (flake de harness, no de suite/producto) | Cycle 185 (CE-122): (a) eliminadas las 23 lineas muertas; suite nueva `createPdfBlob-dead-code-test` 27/27 verifica output PDF byte-identico a la referencia (el scaffolding era inerte). (b) nuevo helper `waitFor(cond, 3000ms)` (poll 10ms, cap 3s) y 9 sitios migrados a esperar flags de completado reales: §1.7 (`saveCompleted`), §3.8-3.10 (`saveDone`), §4.8 (`bgQueuedDone`), §7.1 (`aSaveDone`), §9.5 (`flushDone`), §9.8 (`visFlushed`), §9.11 (`saveOrder.includes('done')`), §10.4 (`dupDone`), §10.17 (`dupDone && srcDone`); las races de negocio se conservan. 3 corridas aisladas 120/120 + gate OK. RELEASE GATE completo PASS 0 fail, total **79 suites segun el manifest del gate** (`release-gate-f19b5b6...json`); los conteos 82/83/84 de ciclos previos no coinciden con sus manifests (77/78) — el manifest es la fuente autoritativa (ver nota en STATUS Cycle 185). Commits `f19b5b6` + `3d0ea88`. |
| CE-123 | P3 | DONE | Higiene de pruebas/cobertura | Deuda de cobertura: ~50 suites huerfanas en `tests/workspace/` (creadas en fase 3 y AW-081..100) NO estaban registradas ni en `tests/run-all.mjs` ni en `scripts/test-workspace-release.mjs`; su regresion era invisible para el gate. 4 tenian la deuda de harness CE-065 (`WORKFLOW_DEFINITION_VERSION` sin resolve tras `stripImports`) y 2 mas tenian probes de fuente imprecisos/rotos | Cycle 186 (CE-123): DISCOVERY 18va ronda = clasificar TODAS las suites huerfanas en aislamiento (35+ corridas). Se rescataron y registraron en el gate **30 suites** (24 pure/VM + 4 E2E browser rapidos + encoding-audit): workflow-lifecycle 96, workflow-persistence 108, workflow-fase3-extended 108, workflow-fase3 64, workflow-templates 53, instruction-assistant-ui 39, workflow-model 39, workflow-batch-scale 25, deep-regression 24, session-recovery 24, operation-registry 26, locale-parser 75, innerhtml-structure 27, history-manager 28, migration-compat 30, workflow-security 19, workspace-storage 19, concurrency 16, workflow-validator 15, workflow-cancel-retry 15, model-fk 17, job-queue 17, error-manager 13, workflow-lifecycle-leak 8, tabular-text-parser 7, encoding-audit 1, plus E2E: phase3-wsp022-svg-security 24, workflow-builder-a11y 11 (fix de regex `/graf/i`→`/gr[aá]fic/i`), tabular-detection 21, workflow-chart 15. 5 fixes de harness/probe: concurrency-test (+`execution-resources.js` al sandwich VM), workflow-model-test y instruction-assistant-ui-test (+`schema-versions.js`), workspace-storage-test (probe estatico stale -> verifica import + `SESSION_SCHEMA_VERSION===1` + uso en registro), workflow-builder-a11y (regex acento). DEFERIDAS documentadas: E2E browser pesados (`phase3-integrity`, `phase4-*`, `phase5-bundle-trust`, `phase6-network-negative`, `step8-validation`, `workflow-e2e`, `instruction-e2e`, `invoice-fields-e2e`, `pdf-image-embed-e2e`, `lazy-capture-images-e2e`, `workspace-tabs-a11y`, `workspace-stability-e2e`, `workflow-chart` NO — este si entro; `phase3a-manual-verification`), diagnostico OCR (`ocr-*`, `perspective-bench`, `playwright-render`), `production-validation` (exige servidor en :8080), helper `idb-helpers`. RELEASE GATE completo PASS 0 fail, total **109 suites** (`release-gate-f29eca41...json`). Commit `f29eca4`. |
| CE-124 | P3 | DONE | Higiene de pruebas/cobertura | Tras CE-123 quedaban E2E browser huerfanos en `tests/workspace/` sin registrar en el gate: `invoice-fields-e2e`, `pdf-image-embed-e2e`, `workflow-e2e-test`, `instruction-e2e-test`, `workspace-tabs-a11y-test`, `workspace-stability-e2e-test` y `lazy-capture-images-e2e` (candidatos inmediatos de la DISCOVERY 19va ronda por runtime presuntamente <30s) | Cycle 187 (CE-124): clasificados los 7 en aislamiento. RESCATADOS y registrados en el gate (total 116 suites): invoice-fields-e2e 16/16 (OCR real), pdf-image-embed-e2e 16/16 (JPEG/PNG/WebP), workflow-e2e-test 31/31 (batch, fallo parcial, cancelacion, persistencia, resultados al Workspace), instruction-e2e-test 17/17, workspace-tabs-a11y-test 22/22, workspace-stability-e2e-test 9/9, lazy-capture-images-e2e 12/12. 3 fixes de harness honestos (sin tocar producto): workflow-e2e-test usaba el regex `/Anadir operacion/` SIN acentos en E2E5/E2E6 (el label real del producto es «Añadir operación», mismo patron que CE-123 con `Gráfico`; E2E1 ya usaba la forma acentuada); workspace-stability-e2e-test NO arrancaba su propio servidor (daba `ERR_CONNECTION_REFUSED`: ningun runner lo servia) — se le anade server autocontenido como las demas E2E del gate; lazy-capture-images-e2e saltaba con scrollTop al fondo y las tarjetas del medio nunca intersectaban el IntersectionObserver (14/24) — el barrido se cambio a pasos incrementales con doble barrido (es el contrato real de lazy-load: carga cuando el usuario pasa por la tarjeta). DEFERIDAS (se mantienen fuera): E2E pesados (`phase3-integrity`, `step8-validation`, `phase4-*`, `phase5-bundle-trust`, `phase6-network-negative`), diagnosticos OCR (`ocr-*`, `perspective-bench`, `playwright-render`), `production-validation` (servidor externo en :8080), `phase3a-manual-verification`, helper `idb-helpers`. RELEASE GATE PASS 0 fail, total **116 suites** (`release-gate-33fb3c3...json`). Commit `33fb3c3`. |
| CE-125 | P3 | DONE | Higiene de pruebas/cobertura | Cierre del censo total de `tests/workspace/*.mjs` contra el gate (DISCOVERY 20va ronda): tras CE-123 (30) y CE-124 (7 E2E) quedaban 14 archivos sin registrar; clasificar TODOS con su razon, rescatar el unico candidato real restante y evitar que la deuda huerfana vuelva a crecer en silencio | Cycle 188 (CE-125): **1 suite rescatada** — `phase3a-manual-verification` 41/41 (10 escenarios del scanner con imagenes programaticas SIN OCR real, scanner UI + behavioral: corner dragging/clamping, confirm guarda PNG corregido, cancel no crea asset, Escape/Ctrl+Enter, object URL cleanup, 5 open/close sin crecimiento, viewport movil, sin errores JS), registrada en el gate (CE-124 la habia clasificado mal como deferida). **Guard permanente** `scripts/audit-orphan-suites.mjs` registrado en el gate: todo `tests/workspace/*.mjs` debe estar registrado en el gate o tener una razon en `DEFERRED_REASONS`; una suite nueva sin registrar ni whitelistear FALLA el gate. Censo actual: 129 archivos = 116 registradas + 13 deferidas documentadas (helper `idb-helpers`; diagnosticos `ocr-word-confidence`, `ocr-reliability-diagnostic`, `ocr-difficult-measurement`, `perspective-bench`, `playwright-render`; E2E pesados `phase3-integrity`, `phase4-integrity`, `phase4-migrations`, `phase5-bundle-trust`, `phase6-network-negative`, `step8-validation`; `production-validation` por servidor externo :8080). **Higiene** `.gitignore`: `_tl*.log` (12 logs de corrida) y `artifacts/deep-audit/release-gate/` (52 manifests stale). RELEASE GATE PASS 0 fail, total **118 suites** (`release-gate-09cc9f3...json`). Commits `09cc9f3`. |
| CE-126 | P2 | DONE | Datos/correctitud | El sort de una tabla (modal de orden y click en cabecera) reordenaba SOLO `table.rows` con `.sort()`, dejando `table.cellConfidence` en el orden antiguo. Tras ordenar, la revision de celdas de baja confianza señalaba celdas EQUIVOCADAS; ademas `snapshotDataTable` no incluia `cellConfidence`, asi que undo/redo re-desincronizaba el par. Sintoma reproducible: ordenar por columna con una celda marcada de confianza baja mueva la marca a otra fila | Cycle 190 (CE-126, DISCOVERY 21va ronda, promovida a tarea del ciclo): nuevo helper `sortTableRows(table, compareRows)` (workspace.js) que ordena filas con el MISMO indice que ordenaria la confianza: construye `[ {row, conf} ]`, ordenta por `compareRow` y reescribe ambas matrices; con `cellConfidence` de longitud distinta o ausente NO la toca (sin crash, sin inventar matriz). Los DOS call-sites de sort (modal `sortDataTable` + listener de click de cabecera `sortTable`) delegan en el helper. `snapshotDataTable` clona tambien `cellConfidence` (copia profunda) y `restoreTableSnapshot` la restaura si el snapshot la trae (`Array.isArray`), conservando compatibilidad con snapshots legacy (que siguen restaurando sin tocar confianza). Suite nueva `tests/workspace/table-sort-confidence-test.mjs` 27/27 (CODIGO REAL por vm.runInContext + anclas estaticas): asc/desc reordenan confianza al mismo orden, pares celda-confianza intactos (Ana->90 etc.), europeos 1.3<1.200 con confianza alineada, sin/mismatched/null cellConfidence sin crash, snapshot clona deep, restore vuelve al pre-sort y llama autoSaveTable, undo/redo de sort mantienen filas+confianza juntas, anclas anti-regresion (2 sitios usan sortTableRows, 0 `table.rows.sort` restantes, snapshot incluye cellConfidence, restore la aplica). Registrada en el release gate. RELEASE GATE PASS 0 fail, total **120 suites** (`release-gate-4728557...json`). Commit `4728557`. |
| CE-127 | P2 | DONE | Producto/informes | Los botones "Informe" del documento (2 call sites en workspace.js ~3759 y ~3779) aplastaban todos los bloques a UNA seccion de texto (`Text Section`) via `doc.blocks.map(b => b.content)`; para un documento con secciones mixtas el informe ya no reflejaba la estructura `documento → tabla → gráfico → informe` del Star-Flow. `documentBlocksToSections` (`workflow-operations.js:631-676`) ya reconstruia secciones por la ruta `document.to-pdf` — no se exportaba ni el botón la usaba | Cycle 193 (CE-127, DISCOVERY 21va ronda -> DONE, PRODUCT_IMPROVEMENT/BUG_FIX P2): `documentBlocksToSections` ahora se exporta desde `workflow-operations.js`. `workspace.js` gana el helper puro `documentBlocksToReportSections(blocks)` (delega en `documentBlocksToSections` y enriquece cada seccion al shape de design-report via `createReportSection`: id/style/assetId, y copia data/dataUrl/width/height). Los 2 botones Informe (pestana Insertar > Salida y pestana Vista > Crear informe) ya usan el helper. Un documento mixto (heading1 + parrafo + bullet-list + table + chart + image-block + quote + divider) → 8 secciones (title/text/table/chart/image/text/divider) en vez de 1 Text Section con todo concatenado. Suite nueva `tests/workspace/report-info-from-doc-test.mjs` 26/26 (VM puro: `documentBlocksToSections` y `createReportSection` reales): doc Star-Flow mixto 8 secciones, table conserva data.headers/rows, chart conserva data.series, image conserva dataUrl/width/height, bullet/quote marcados (`• ` / `> `), shape completo todas las secciones, heading2->subtitle/heading3->text, doc vacio fallback text '', doc solo-texto equivalente al flujo antiguo, y 8 anclas estaticas (export de la funcion, import en workspace.js, 2 call-sites con el helper, flatten viejo fuera). Registrada en el release gate tras CE-128. Relieves sin regresion: `workflow-document-pdf` 75/75, `design-report-wysiwyg` 14/14, `document-export-md-lists` 23/23, `export-md-chart` 26/26, tabular-detection E2E 21/21 (anclas createReportConfig/createReportSection intactas). Censo CE-125 OK (134 = 121 registradas + 13 deferidas). RELEASE GATE PASS 0 fail, total **123 suites** (`release-gate-c08b59f...json`). Commits `c08b59f` (codigo) + docs. Limitaciones: el primer heading1 se convierte en seccion `title` (nunca como subtitle); bullet-list/quote se conservan como texto marcado (no como bloques de lista nativos del builder de diseño). |
| CE-128 | P2 | DONE | Datos/export | `exportDocumentMarkdown` (workspace.js ~4137-4183, export del toolbar) NO tenia `case 'chart'` (y tampoco delega en `blocksToMarkdown`), asi que los bloques de grafico caen al default de contenido y el Markdown exportado perde la estructura `documento → tabla → gráfico → informe` del Star-Flow; la ruta de Flujos `blocksToMarkdown` SI los cubre (asimetria entre rutas de exportacion Markdown) | Ciclo 192 (CE-128, DISCOVERY 21va ronda -> DONE, BUG_FIX): `exportDocumentMarkdown` ganó `case 'chart'` replicando la paridad exacta de `blocksToMarkdown` (workflow-operations.js:706-717): fence ```` ```charts ```` con titulo `(block.content || 'Grafico')` + tabla `| Etiqueta | Valor |` con separador y filas `item?.label` (escapando `|`) / `item?.value` (String(value ?? '')), table solo si `series` es array no vacio; sin series solo fence+titulo. Suite nueva `tests/workspace/export-md-chart-test.mjs` 26/26 (REAL puro vía VM como CE-111, sin DOM): fence/titulo/header/separador/series, pipe escapado en etiqueta, default 'Grafico', sin series sin tabla ni crash, value ausente -> celda vacia, mezcla ordenada, PARIDAD directa contra `blocksToMarkdown` REAL (grabFn + fence/fenceEnd vía VM), y 3 anclas estaticas (exportDocument delega, `type === 'chart'`, chart despues de image-block y antes del fallback). Registrada en el release gate tras CE-130. Regresiones relacionadas OK: `document-export-md-lists` 23/23, `workflow-export-md` 30/30, `md-header-escape-and-replace` 12/12, `text-to-document` 15/15. Censo CE-125 OK (133 = 120 registradas + 13 deferidas). RELEASE GATE PASS 0 fail, total **122 suites** (`release-gate-9b24cee...json`). Commits `9b24cee` + `ef4...` (docs). |
| CE-129 | P2 | DONE | Datos/edicion | La vista de tabla no permite eliminar columnas ni filas (grep 0 de removeColumn/removeRow); el usuario de una tabla OCR no puede corregir lineas/columnas espurias sin salir a herramientas externas | Cycle 194 (CE-129, DISCOVERY 21va ronda, promovida a tarea del ciclo): helpers puros `removeTableColumn(table, index)` y `removeTableRow(table, index)` en workspace.js. Borrar columna: splice de `headers`, cada `row`, `cellConfidence` (por columna) y `columnTypes`; `_colFilters` se re-indexa (los filtros de columnas posteriores se desplazan una posicion, el de la columna borrada se retira; los `Set` se conservan por referencia). Borrar fila: splice de `rows` y de `cellConfidence` (por fila); `headers`/`columnTypes`/`_colFilters` intactos (indexados por columna). Guard: tabla sin headers/rows, indice fuera de rango/NaN/negativo -> `false` sin mutar; minimo viable 1 columna y 1 fila (no se puede borrar la ultima). `snapshotDataTable` ampliado: ahora clona tambien `columnTypes` (shallow) y `_colFilters` (`cloneColFilterMap`: `Set`->`Set`, array->array) para que el undo/redo de un borrado restaure el estado COMPLETO; `restoreTableSnapshot` los restaura solo si el snapshot los trae (los snapshots legacy siguen funcionando). UI: nuevo grupo "Eliminar" en la cinta Datos de la vista de tabla con 2 botones — "Eliminar fila" (usa `selection.focusRow`) y "Eliminar columna" (usa `selection.focusCol`) - ambos exigen seleccion (toast si no), abren `showModal` de confirmacion con el nombre de columna / numero de fila, y aplican `checkpointTableEdit` -> helper -> `commitTableEdit` -> `autoSaveTable` -> `rerenderTable` (undo/redo completo con Ctrl Z/Y). Suite nueva `tests/workspace/table-remove-column-row-test.mjs` 51/51 (funciones REALES por VM grabFn): columna media/primera/ultima (headers+filas+confianza+columnTypes+filtros), re-index de filtros desplazados, retiro del filtro de la columna borrada, fila media (headers/filtros/tipos intactos), guards (indices invalidos, sin mutacion, minimo 1x1, bucle 3x3->1x1 bloqueado en el ultimo), snapshot clona columnTypes+Sets, restore completo llama autoSave y vuelve al estado original, restore legacy conserva campos vivos, undo logico completo, 8 anclas estaticas. Registrada en el release gate tras CE-127. Relieves sin regresion: `table-sort-confidence` 27/27 (CE-126), `col-filters-roundtrip` 38/38 (CE-130), `table-history-cap` 8/8, `undo-corruption` 15/15, `workspace-test` 157/157. Censo CE-125 OK (135 = 122 registradas + 13 deferidas; CE-129 registrada). RELEASE GATE PASS 0 fail, total **124 suites** (`release-gate-34c99ce...json`). Commit `34c99ce` (codigo) + docs. Limitaciones: (1) minimo 1 columna y 1 fila (una tabla vacia se elimina entera, no se reduce a 0x0); (2) los filtros re-indexados comparten la referencia del Set (no se clonan al borrar columna; si se clonan al hacer snapshot/undo); (3) undo de un borrado NO restaura `reviewStatus`/`sheet` de la tabla (no forman parte del snapshot de tabla); (4) el borrado actua sobre fila/columna de la CELDA seleccionada, no hay menu contextual sobre la cabecera. |
| CE-130 | P1 | DONE | Import/Export | Los filtros de columna viven en `table._colFilters[ci]` como objetos `Set`; `JSON.stringify(new Set(...))` los serializa como `{}`, asi que tras exportar/importar un proyecto la vista de tabla lanza `TypeError: colFilter.has is not a function` al aplicar el filtro | Cycle 191 (CE-130, P1 de la DISCOVERY 21va ronda, promovida a tarea del ciclo): fix bilateral sin cambiar el modelo en memoria. `core/table-helpers.js` gana `colFiltersToSerializable(table)` (proyecta `_colFilters` a arrays ordenados JSON-safe, sin mutar), `normalizeColFilters(table)` (array->Set, conserva Set, descarta formas invalidas como el `{}` corrupto) y `colFilterHas(colFilter, value)` (una forma corrupta = sin filtro, la fila pasa; usado por checkbox y loop de filas del render). `core/storage.js`: `exportProject` serializa los filtros en el bundle y `importProject` normaliza a Set tras el remap (el `{}` producido por `JSON.stringify(new Set)` pasaba el hash de integridad porque `canonicalJson` hashea el MISMO objeto en ambos lados). Suite nueva `tests/workspace/col-filters-roundtrip-test.mjs` 38/38 (helpers reales; export/import REAL con fake-indexeddb + round-trip JSON; legacy corrupto de un archivo pre-fix no lanza y descarta el filtro; anclas estaticas en los 3 archivos). Los harnesses de storage en VM (CE-115, CE-061 x2, CE-059 x2) anadieron `core/table-helpers.js` a su sandwich (dependencia nueva de storage.js). RELEASE GATE PASS 0 fail, total **121 suites** (`release-gate-caac30b...json`). Commit `caac30b`. |
| CE-131 | P2 | DONE | Producto/workspace | Duplicar documento/tabla/captura en sitio: el patrón "duplicar proyecto" de CE-086 declara la duplicación de entidades como vía futura y ya tiene los primitivos. Para duplicar una CAPTURA el requisito duro es NO duplicar la imagen grande: reusar el mismo `correctedAssetId` (imagen compartida entre original y duplicado, patrón CE-029) y solo copiar los metadatos id/relations | Cycle 195: `cloneDocEntity`/`cloneDataTableEntity`/`cloneCaptureEntity` (helpers puros) + `duplicateDocCard`/`duplicateDataTableCard`/`duplicateCaptureCard` con botón `Duplicar` (icono copy) en las tarjetas de Documentos/Datos/Capturas. La tabla clonada NO hereda `_colFilters` (nace sin filtros) y el documento no hereda vínculos de origen (copias editables independientes); la captura reusa `correctedAssetId`/`sourceAssetId`/`scanDocumentId` y declara relación `asset` con el asset compartido (sin crear assets nuevos). Suite `duplicate-entities-test` 51/51; gate 125 suites PASS 0 fail, manifest `release-gate-0341439...json` |
| CE-132 | P3 | DONE | Correctitud/editor | El slash-menu "/" crea bloques de tipo `list` (inválido; los tipos válidos son `bullet-list` y `numbered-list`) que el render/export degradan silenciosamente a texto plano con la viñeta quemada en el contenido | Cycle 197: el item "Lista" del `slashItems` pasa de `type: 'list'` a `type: 'bullet-list'` (único call-site que creaba el tipo inválido; la línea 8931 `['subtitle',...,'list']` es un icono de sección LAF, no un tipo de bloque). El render ya trataba `bullet-list` con placeholder propio y `exportDocumentMarkdown` ya lo emitía como `- content` (CE-111), así que el fix restaura la paridad completa slash-menu → editor → export → informe. Suite `slash-menu-bullet-list-test` 16/16 (array `slashItems` REAL extraído del source con verificación de tipos; anti-regresión: 0 ocurrencias de `type: 'list'` en workspace.js, `BLOCK_TYPES` conserva `bullet-list`/`numbered-list` y sin `list`, click/render/save intactos, paridad con export y createTextBlock). Gate 127 suites PASS 0 fail, manifest `release-gate-54a4c85...json` |
| CE-133 | P3 | DONE | Dashboard | Los widgets de dashboard son inmutables tras crearse: `openDashboardWidgetModal` solo crea (no edita titulo/categoria/campo) y los controles de panel no dan opción de editar; `normalizeWidget` no acota `chartTable` contra `headers.length` (puede apuntar a indice fuera de rango) | Cycle 196: helper puro `dashboardClampWidgetColumns(widget, headersLength)` (no muta; `field` fuera de rango/negativo/no entero -> `''` = conteo de filas, `category` -> 0, `columns` filtradas y derivadas `[0..5)` si no vienen, `width = max(0, Number(headersLength))`). `dashboardNormalizeConfig` delega el clamp por widget (antes solo `columns` inline; ahora `field`/`category`/`columns` acotados contra `headers.length` al reabrir). `openDashboardWidgetModal(source, commitConfig, existing)` con modo edición: precarga title/type/category/field/aggregate, títulos "Editar visual"/"Guardar cambios" y reemplaza por id (mutación, sin re-push). Botón `Editar visual` (icono edit) en `renderDashboardWidget`. Suite `dashboard-widget-edit-test` 42/42 (helper puro real + normalize con stubs inyectados de constantes/`queryNumber`/`generateId` + anclas estáticas del modal); gate 126 suites PASS 0 fail, manifest `release-gate-0ed8dcb...json`. Límite documentado: la edición no permite reordenar columnas de una tabla de detalle (solo conserva las ya elegidas); el título vacío vuelve al placeholder. La DISCOVERY original hablaba de `normalizeWidget`/`chartTable` (no existen) — la normalización real vive en `dashboardNormalizeConfig` |
| CE-134 | P3 | DISCOVERED | UX/navegacion | No hay cambio rápido de proyecto desde la barra superior: el breadcrumb del nombre de proyecto es un `<span>` no clickeable (solo se sale del proyecto por el sidebar) | Ciclo 189 (DISCOVERY 21va ronda, explorador de proyectos). Oportunidad: selector rapido de proyecto en el topbar |
| CE-135 | P3 | DONE | UX/capturas | No se puede descargar/exportar una captura individual como imagen desde su tarjeta (solo ocr/delete); `resolveCaptureImageDataUrl` (workspace.js) ya resuelve el dataUrl de la captura | Cycle 198: helper puro `captureDownloadFileName(capture, dataUrl)` (sanea el nombre con `replace(/[\\/:*?"<>|]/g, '-')`, recorta guiones finales, cae a `captura-<id>`/`captura` si el nombre es vacío, y deriva la extensión del MIME del dataUrl: `jpeg`/`jpg` -> `.jpg`, `png`/`webp`/`gif`/`avif` tal cual, default `.png`). Botón `Descargar` (icono download) en cada tarjeta de captura (`renderCaptureView`): `resolveCaptureImageDataUrl(cap, loadAsset)` -> `fetch(imageUrl).blob()` -> `URL.createObjectURL` -> ancla download con `captureDownloadFileName(cap, imageUrl)` -> `revokeObjectURL`; avisa si la captura no tiene imagen y reporta `capture-download` en fallo. Suite `capture-download-test` 25/25 (helper REAL + anclas estáticas del manejador); gate 128 suites PASS 0 fail, manifest `release-gate-d7591d5...json` |
| CE-136 | P2 | DONE | Flujo estrella/informe | Un salto de página insertado en un párrafo descarta todo el texto del bloque en informe/PDF/Markdown: `insertDocPageBreak` (workspace.js:3650) inyecta `<div data-page-break="true"></div><p></p>` DENTRO del contentEditable del bloque actual, y las rutas de export trataban cualquier bloque cuyo html contuviera el marcador como si fuera SOLO un salto (`exportDocumentMarkdown` workspace.js y `documentBlocksToSections` workflow-operations.js lo truncaban a una única sección page-break). El HTML sí conservaba el texto → divergencia de artefactos | Cycle 199 (DISCOVERY 22va ronda, hallazgo calidad F1): helpers puros `splitHtmlAtPageBreak(html)` (divide el html del bloque por cada divisor `data-page-break="true"` conservando texto antes/después; descarta segmentos vacíos) y `htmlFragmentToText(html)` (extrae texto plano decodificando entidades). `documentBlocksToSections`, `blocksToMarkdown` y `exportDocumentMarkdown` recorren los segmentos y emiten texto + salto + texto (nunca descartan contenido); un bloque SOLO-marcador sigue produciendo una sección page-break (sin regresión). Suite nueva `page-break-text-preserved-test` 31/31 (helpers y `documentBlocksToSections` REALES vía grabFn + anclas estáticas de las 3 rutas); relieves CE-127/CE-128/CE-111/CE-106/CE-132/design-report/editor intactos (3 suites actualizadas para inyectar los 2 helpers nuevos que ahora dependen). Gate **129 suites PASS 0 fail**, manifest `release-gate-72d1015...json`. Commits `72d1015` |
| CE-137 | P2 | DONE | Producto/OCR | `faithfulOcrText` (workspace.js:3051, 3158–3176) es código muerto: la promesa "resalta palabras dudosas del OCR" nunca renderiza; `doc.ocrWords` se persiste pero nada lo lee; la clase CSS `ws-ocr-low-confidence` solo existe en la función muerta | Cycle D-18 (DEAD_CODE, P2): la funcion muerta y la clase CSS se eliminaron en 5a3629f (recuperacion de presupuesto W6) pero CE-137 quedo DISCOVERED; D-18 la cierra con evidencia: (1) 0 ocurrencias de `faithfulOcrText` en workspace.js y 0 de `.ws-ocr-low-confidence` en workspace.js/.css; (2) limpieza de la condicion redundante en `finalizeExtraction` (`if (mode === 'faithful' && words...) doc.ocrWords = words` subsumida por la linea general — dedup a UNA escritura); (3) certifica la cadena VIVA `doc.ocrWords -> ocrWordConfidenceMap -> buildCellConfidenceMatrix -> table.cellConfidence` (convertDocToTable construye la matriz al convertir documento a tabla) con OCR_LOW_CONFIDENCE en revision/UI. Suite nueva `tests/workspace/ocr-low-confidence-dead-code-test.mjs` 23/23 (anclas estaticas + comportamiento REAL via grabFn: normalizeOcrNumber signo 1-30→-30, ocrWordConfidenceMap fusion de claves/min confianza, buildCellConfidenceMatrix con fallback ocrConfidence, celda vacia→null, equivalencia numerica europea 1.500,25 vs 1500,25, multi-token→min). Registrada en el gate. Regresion enfocada OCR/revision: table-sort-confidence 27/27, doc-to-table-multispace 20/20, review-status-persistence 15/15, table-sort-locale 16/16, locale-parser 75/75. RELEASE GATE completo 135 suites PASS 0 fail. Commit `6893cff` |
| CE-138 | P2 | DISCOVERED | Producto/editor | Los bloques `chart` creados por `data.to-chart`/`report.create` son invisibles en el editor de documentos: `renderBlock` los degrada a texto plano, `BLOCK_TYPES` no incluye `chart`; export/PDF sí los manejan → asimetría editor vs artefactos | Hallazgo 22va ronda (explorador de producto H2). Potencial: render real del chart en el bloque |
| CE-139 | P1 | DISCOVERED | Rendimiento | `rerenderTable` (workspace.js:5259) reconstruye el grid completo (hasta ~250k `<td>` + ~500k listeners) en cada paste/delete/sort/filtro/fórmula/undo | Hallazgo 22va ronda (explorador de rendimiento R1). Esfuerzo L; requiere ciclo dedicado |
| CE-140 | P1 | DISCOVERED | Rendimiento | `commitTableEdit`/`checkpointTableEdit` (workspace.js:5040–5044, 5131–5136) hacen 2 deep-clones + 4 `stringify` de TODA la tabla por cada edición de celda | Hallazgo 22va ronda (explorador de rendimiento R2). Esfuerzo L |
| CE-141 | P3 | DISCOVERED | Producto/export | `exportTableCSV` escribe las fórmulas en bruto (`=SUM(...)`) mientras la vista muestra el valor evaluado (`evaluateDataFormula`) | Hallazgo 22va ronda (explorador de producto H3). Decisión de diseño: valor calculado vs fórmula para re-import |
| CE-142 | P3 | DISCOVERED | Persistencia/editor | El undo/redo del documento (`_applyState`) restaura solo el appStore en memoria; un Ctrl+Z que deshace cambios ya guardados no persiste el estado deshecho en storage | Hallazgo 22va ronda (explorador de calidad F2). Esfuerzo S |
| CE-143 | P3 | DONE | Informe/export | `documentBlocksToSections` degrada `numbered-list`/`code`/`callout` a texto plano en el informe/PDF, mientras otros tipos conservan su semántica (bullet/quote sí) | Cycle D-19 (FEATURE, P3): `documentBlocksToSections` (workflow-operations.js) ahora conserva la semántica: `numbered-list` → un section por `li` con prefijo numérico secuencial (`1.`, `2.`...), `code` → contenido crudo sin pérdida, `callout` → `> Nota: <texto>` (mismo patrón que quote); `pdf-generator.js` y `workspace.js` mantienen soporte coherente en las rutas de export. Suite nueva `tests/workspace/document-blocks-sections-semantics-test.mjs` 39/39 (comportamiento REAL de `documentBlocksToSections` via grabFn para numbered-list/code/callout/bullet/quote/heading/paragraph/page-break, registrada en el gate). De paso se recortó el presupuesto de dist: 11 clases CSS muertas eliminadas de `workspace.css` (214315 → 208678 bytes) → `workspace-test 157/157` con `Total workspace dist size: 1194KB` (PASS <1200KB). RELEASE GATE completo 136 suites PASS 0 fail. Commit del fix: ver STATUS Cycle D-19 |
| CE-144 | P3 | DISCOVERED | Datos/clonado | `cloneDataTableEntity` (CE-131) clona `columnTypes` con spread `{...}` sobre un ARRAY → produce un objeto; el shape real usa array; la regresión se auto-corrige en carga pero es código que clona mal | Hallazgo 22va ronda (explorador de calidad F8). Esfuerzo S |
| CE-145 | P2 | DONE | Rendimiento/Query | `queryRunOperation` hace hasta 3 barridos de filas por paso (detect-type + normalize + rebuild) y `markTableSelection` recorre toda la selección O(N) en cada tecla — Hallazgo 22va ronda (R3/R4). Esfuerzo M; dividida en D-15: R4 (markTableSelection) cerrada, R3 (barrido triple de queryRunOperation) queda como CE-149 | Cycle D-15 (CE-145 R4, PERFORMANCE_IMPROVEMENT): `markTableSelection` (workspace.js) pasa del barrido completo `$$('td[data-row][data-col]')` en cada tecla a un marcado incremental O(celdas cambiadas): nuevos WeakMaps de modulo `_tableSelectionMarks` (estado por tableEl: bounds + focus + tbody) y `_gridRowIndex` (indice fila→tr por tbody, registrado en renderGrid); helpers `sameSelectionRect`, `tableSelectionDiff` (diferencia simetrica en bandas disjuntas, max 8 rectos; invarianza area(on)-area(off)==area(curr)-area(prev)) y `forEachGridCellInRect`/`gridRowLookup`/`gridCellAt`. La ruta incremental se usa cuando el tbody es el mismo y el rectangulo cambio (flechas/Tab/Home/End/Shift-extend/Ctrl+A): solo toca las celdas cuyo estado cambia + transicion de foco (selected-focus); el barrido completo queda de fallback para el primer marcado o un tbody recien reinstalado. Suite nueva `tests/workspace/table-selection-incremental-test.mjs` 28/28 (anclas estaticas + tableSelectionDiff/sameSelectionRect REALES ejecutados via grabFn). Regresion: data-table-rerender-scope 35/35, data-table-undo-scope 10/10, table-history-cap 22/22, table-remove-column-row 51/51, workspace-test 157/157 (dist 1192KB < 1200KB), workspace-stability-e2e 9/9. RELEASE GATE completo 133 suites PASS 0 fail (manifest `release-gate-01c0765...json`). Commit `01c0765`. Hallazgo anexo: Ctrl+A solo marca la celda (0,0) — registrado como CE-148. |
| CE-148 | P2 | DONE | Datos/seleccion | Ctrl+A en la vista de tabla NO selecciona toda la tabla: el handler (workspace.js ~L5627) hace `setSelection(0,0,false)` — que fija anchor Y focus en (0,0) — y luego rellena `selection.endRow/endCol`, pero `tableSelectionBounds` solo lee `anchorRow/focusRow/anchorCol/focusCol`, asi que el barrido solo pinta la celda esquina; el Ctrl+C posterior copia 1 celda en vez de toda la tabla | Cycle D-17 (BUG_FIX, P2): `workspace.js` — la rama Ctrl+A del keydown de la tabla hace `setSelection(0, 0, false)` (ancla la esquina) y, si `table.rows.length >= 1 && table.headers.length >= 1`, `setSelection(table.rows.length - 1, table.headers.length - 1, true)` (mueve el focus REAL al final): `tableSelectionBounds` ahora devuelve (0,0)-(rows-1,cols-1) y `selectedTableTsv` copia TODAS las celdas en TSV. Eliminadas las escrituras muertas `selection.endRow/endCol` (2 escrituras, 0 lecturas) y la llamada manual a `markTableSelection` (setSelection la invoca). Guard de tabla sin filas: bounds de 1 celda segura, sin indices -1. Suite nueva `tests/workspace/table-select-all-test.mjs` 16/16 (anclas estaticas del branch + regresion del mecanismo muerto + comportamiento REAL de `tableSelectionBounds`/`selectedTableTsv` via grabFn en 3x4, 1x1 y sin filas). Registrada en el gate. Regresion enfocada: table-selection-incremental 28/28, data-table-rerender-scope 35/35, data-table-undo-scope 10/10, workspace-test 157/157. RELEASE GATE completo 134 suites PASS 0 fail. Commit `3ad2fdd` |
| CE-149 | P2 | DONE | Rendimiento/Query | Resto de CE-145 (R3): `queryRunOperation` hace hasta 3 barridos de filas por paso (detect-type del boton + normalize + rebuild); fusionar/evitar barridos redundantes para ediciones de una celda | Cycle D-16 (CE-149 R3, PERFORMANCE_IMPROVEMENT): `workspace.js` — (1) el ramo de text-ops (trim/clean/uppercase/lowercase/detect-type) ya no re-clona (`rows.map(row => [...row])`) ni recorre por columna fuera de lugar: `result.rows` YA es un clon profundo de `queryCloneShape`, asi que la transformacion muta en sitio con un solo `forEach` de filas y `selected.forEach` por columna dentro (fill-down/fill-up conservan su pase secuencial por columna con la misma semantica); (2) `queryApplyStep` aplica SOLO el paso nuevo con `queryRunOperation(model, operation, config)` sobre el modelo materializado y asigna headers/rows (O(filas) por paso) en lugar de `queryRebuildModel` (replay de TODA la cadena, O(filas x pasos)); `queryRebuildModel` sigue siendo autoridad para undo/reset/quitar paso y el restore (`queryModelFromSaved`) re-hace replay — sin divergencia tras recargar. Suite nueva `tests/workspace/query-single-pass-test.mjs` 20/20: anclas estaticas (sin 2do clon, transform in-place, applyStep sin replay) + equivalencia incremental==replay en cadenas reales (detect-type+filter+sort, uppercase multi-columna+add-index+dedup) y en CADA aplicacion intermedia (n=1..4) + aislamiento de mutacion del origen + fill-down/up secuenciales + undo sigue autoritativo; usa `parseLocaleNumber` real por `grabFn` de `core/locale-parser.js`. Registrada en el gate (censo CE-125 OK). RELEASE GATE completo 133 suites PASS 0 fail. Commit `f2616cb` |
| SEO-01 | P2 | DONE | Sitio publico (apluno.com): cobertura de Google Search Console — deindexacion masiva 2026-08-28 (indexadas 54, sin indexar 178; 169 rastreadas-sin-indexar), categoria «Descubierta - aún no rastreada» dominante | Cycle D-19 por directiva del owner: analisis de cobertura con export GSC (ZIP en Downloads) consumido y documentado; hotfix `offline.html` raiz con `<meta name="robots" content="noindex, nofollow">` + description (propagado a dist y verificado tras rebuild); `buildSitemap()` (scripts/generate-seo-pages.mjs) con las 5 editoriales `/guia/` `/about/` `/contact/` `/privacy/` `/terms/` (sitemap 224 URLs). Rebuild + `node scripts/audit-content-quality.mjs` → **FINAL: PASS (pass=15, warn=0, fail=0)** — indexability sin fails. Evidencia en `artifacts/adsense-content-remediation/` (`baseline-audit.md`, `url-inventory.json`, `audit-content-quality.json`). Despliegue sigue esperando canal del owner; push prohibido por politica |
| SEO-02 | P2 | DONE | Sitio publico (apluno.com): Fase 1 remediacion — guia editorial | La seccion editorial `/guia/` YA existia con 10 guias (gate `guides` en verde). Hueco de mayor valor: la familia OCR (flujo estrella «escaneado -> OCR -> tabla -> informe»). Added `src/data/guides.json` → guia `convertir-escaneado-imagen-en-texto-excel` (categoria pdf, ~700 palabras, 3 secciones + FAQ + JSON-LD Article/FAQ auto-generado): cuando funciona el OCR, foto/escaneado → texto plano (`extraer-texto-pdf-escaneado`) o → Excel (`extraer-tabla-imagen-excel`), PDF buscable (`pdf-escaneado-a-pdf-buscable`), deteccion previa (`detectar-ocr-pdf`) y mejora de imagen antes de extraer (`mejorar-documento-escaneado`, `escanear-documento-camara`) — todos enlaces reales verificados. Rebuild completo + `audit-content-quality` **PASS 15/15 (guides 11/11 valid); gate publico `test-public-release` 24 PASS 0 FAIL** (seo 29, adsense 25, monetization 25, strict-editorial 3, content-similarity 5, remediation DoD 11, network-negative 413/413). Sitemap 237 URLs |


## Guia de seleccion

1. Tomar el primer `TODO` ordenado por prioridad (P0 primero, luego P1, P2, P3).
2. Cambiar su estado a `ACTIVE` en esta cola antes de implementar.
3. Si esta `BLOCKED` o `BLOCKED_FLAKY`, leer la causa y elegir la siguiente ejecutable.
4. `DISCOVERED`: oportunidad encontrada durante un ciclo; solo se ejecuta al promoverse a TODO.
5. `DEFERRED`: descartada temporalmente con causa (no cumple hoy los criterios de producto).
6. Al terminar: estado `DONE` con evidencia (tests, commits, artefactos).
7. Si un `DONE` se degrada en una regresion posterior, volver a `TODO` con causa.
8. Si no queda ningun TODO: el ciclo se dedica a DISCOVERY (generar oportunidades nuevas). Promover la oportunidad de mayor prioridad a `TODO` en el ciclo siguiente antes de implementarla.
